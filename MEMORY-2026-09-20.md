# MEMORY-2026-09-20 · checkpoint（12 reds 修真 + P3c/P3d 编排真执行 + register 订正）

## 一、本会话完成的闭环

### 1. 12 个 pre-existing test reds 修真（commit `1ec218c`，已 push）
- 根因统一：`6844ec8` block1 GUI 重构把侧栏 CSS 提取到 `shared-styles.css`、stat 卡改
  `.hig-stat-card blue/green` + `hig-stat-value`、title 简成 `Proxy Manager`，测试文件停在更早
  `f43876c` 没跟。12 个里 **11 个测试过时 + 1 个真 a11y 补齐**（dashboard 缺 `prefers-reduced-motion`）。
- 修真（不放水需求）：`dashboard.html` 恢复 `- Dashboard` title + 补 reduced-motion a11y 块；
  `dashboard.test.js` 2 断言跟到 `.hig-stat-card`；`design-system.test.js` 8 侧栏 + 2 响应式断言重锚 `shared-styles.css`。
- E2E：jest 全量 **44 套 / 701 测试全 PASS**（修真前 12 fail → 0 fail，11 测试过时 + 1 a11y 全解）。

### 2. P3c/P3d：orchestrator 编排「真执行」落地（commit `6bb3827`，已 push）
- **真缺口核实**：`mcp-server.js` 的 `orchestrate`（callToolAsync）构造 `new Orchestrator({shadowMode})`
  **无 executor 注入** → 非 shadow 会抛 `'no executor injected'`（orchestrator.js:158）；`PROXY_ADAPTER_REAL`
  （l.194）是**纯注释、全仓零引用**。即「真执行」此前不存在。
- **落地**（非侵入、门控默认关）：`mcp-server.js` 加 `PROXY_ADAPTER_REAL` **二级门控**——
  - 门控关（默认）= shadow，per-call `shadowMode=false` **不可绕过门控**（防意外触下游）。
  - 门控开 = 真执行：`_realExecutor` 经 `routeEngine.route` 路由选中 adapter → 委托注入 `executor`；
    无注入 executor 时**退化「仅路由」**（`{adapterId, status:'routed', routedAdapterId}`，记录选中 adapter 但不触下游）。
- 新增 `l2/mcp-orchestrate-real.demo.js`（**9/9 E2E**：3 用例 × 真起子进程，UC1 门控开真执行 / UC2 门控关恒 shadow / UC3 真启 stdio）
  + `mcp-server.test.js` **+4 例**（共 **26/26**）+ `mcp-route.test.js` 10/10。
- **教训（本轮·1 条）**：jest UC2 初版断言 `values.every(v => v.routedAdapterId)` 过严——`fastapi-jwt` 模板
  第 4 子任务 type=`test` 但注册表无 `test` adapter → 该子任务退化为 `routed` 且 `routedAdapterId=null`
  （**正确退化，非缺陷**）。修正：`.every` → 区分「全 routed」与「code 类带 codex / test 类 null」。
- 热路径 `forward.js` / `proxy.js` 全程 0 触碰（git diff 核对）。

### 3. EXTERNAL-OPTIONS-REGISTER.md 订正（commit `6bb3827`，已 push）
- **文档债根因**：09-18 旧判断「option3 仍没动 / 项目无 MCP 代码 / jest 837 / option1 进行中(设计草案)」
  与 09-19/09-20 实际矛盾（AGENTS §8「不得把历史误述为当前」风险）。
- **订正**（据实盘，非臆造）：option3 MCP bridge + 真执行已落地 / option1 已实施 + 3 profile seed
  （`agnes/small / deepseek/medium / qwen/large`，已 `mcp-default-seed.js` 落地，jest 14/14）/
  基线 837（09-18）→ **701/701（09-20，44 suites / 0 fail）**。依赖关系图 + 总判定表全部同步。
- `l2/README.md` 注册新 demo `mcp-orchestrate-real.demo.js`（AGENTS §7 文档同步）。

## 二、自纠的一处陈述错误（09-20 发现，已更正）
- 上一轮据 09-18 旧 register 断言「P3c/P3d 未做 / 3 profile 未注册」——**与实盘不符，已纠正**：
  option1 已落地（14/14 + 3 profile seed）；option3 MCP bridge 已落地；真缺口仅「`_execute` 接真实 executor」，本会话已补。

## 三、当前 git 状态（实跑回读验证）
- `origin/main` = 本地 HEAD = **`6bb3827`**，ahead/behind = **0/0**。working tree **clean**。
- 本会话 2 commit 已 push：`1ec218c`（12 reds 修真）+ `6bb3827`（P3c/P3d + register 订正 + README 同步）。
- 热路径零触碰。

## 四、本轮测试基线（实跑，非摘录）
- mpm jest **701/701**（44 suites / 0 fail，含 mcp-server 26 + mcp-route 10 + 其余）
- l2 demo **15/15 全 PASS**（含新增 `mcp-orchestrate-real` 9/9）
- shell 回归：check.sh 15/15 + check-uninstall.sh 25/25（上轮 R2 已 push，本轮未再动）

## 五、阻塞项（维持，待外部条件 / 老板发话）
- **DSH DS2**：等 DSH 0.2 + issue #1496 guardrail，已登记不写码。
- **方向六 + Paseo**：`l2/p3-c1-paseo-integration.md` 三方案 A(维持现状,推荐)/B(外包,等手机续看30min需求)/C(全替代,不推荐)，待 signoff。
- **Marvis P5**：全仓 grep(`marvis|P5|P-5|priority.*5`,*.md+*.js) **0 匹配**——名称/编号需老板澄清，或主体已在「P5 桌面壳」(Electron main.js + manage.sh gui,09-17完成) 闭环，余为 L2-BLUEPRINT 排期表的「Marvis 式工位看板+插件市场 UI」锦上添花(可延后)。
- **option2(Octop harness-agent failover middleware)**：阻塞已解（spec 已出），但实现是 Python middleware（跨语言），待老板发话再实施。
- **option3 二期**：HTTP/SSE 服务 + LLM 拆解真接线（`PROXY_L2_MCP_REAL` 升级，复用 `routes/orchestration.js` 的 LLM 缝）列后续。

## 六、上下文健康
- 本轮工具额度到顶 1 次触发 checkpoint 存档；建议后续开新窗口。
