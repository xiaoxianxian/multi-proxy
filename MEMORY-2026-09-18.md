# MEMORY-2026-09-18 当日日志

> 项目：~/Documents/AI项目/multi-proxy。HEAD `ed138f4` = origin/main，working tree 干净（本轮仅 doc 新增未 commit）。

## 本轮目标（2026-09-18）
1. DeepSeek 两段反馈核实（文档缺 + DSH 接入）
2. 据截图 P0-L2 生产路由接入「先做这个」——真核实 P0 真实状态

## 一、DeepSeek 两段反馈核实

### 第一段（文档缺漏）——全误判
| 报的问题 | 核实 | 结论 |
|---|---|---|
| 3 ADR 引用断裂（`03-architecture/adr/0002-no-proxy-iron-rule.md`/`0003`） | `docs/03-adr/` 3 ADR 全在（0001-lsof-absolute-path 2053B / 0002-no-proxy-iron-law 2828B / 0003-l2-non-invasive-principle 3112B）；全库搜 `03-architecture/adr` 零命中 | **误判**（路径+文件名都写错，实际在 `03-adr/` 且文件名是 `iron-law`/`non-invasive-principle`） |
| risk-register 需确认 | `docs/09-review/risk-register.md` 89L 已存在（R3 `3d3d730` 建） | **已存在** |
| 文档体系评分 ⭐⭐⭐⭐⭐ | 综合已可交付 | 认可 |

### 第二段（DSH 接入）——1 真增值建议
- 判断准确：DS1✅ / DS2·DS3 暂缓（#1496 guardrail + 等 DSH 0.2）；L2 内核设计理念与 Cordis 同构但实现独立运行时。
- **真增值建议**：「等待期提前设计 cordis.patch.yml / dsh.bundle 补丁结构（参考 fan56/dsh-llm-proxy / dsh-plugin-model-proxy）」。
- **已落 `docs/02-product/bundle-design.md`**（4881B）：官方规范（`deepseek-ai/deepseek-harness/packages/bundle/base/cordis.patch.yml`：`insert:` 块 + `id/name/config`，`name:`非`module:`，`dsh.bundle.patch` 自动注入，`dsh.client` 自动发现）+ 参照 `dsh-plugin-model-proxy`（per-model 路由，与 L2 同构）+ 8 条社区踩坑 + L2 接入映射 + 落地前置（DSH 0.2）。**纯设计非实施，不破坏 DS2 暂缓**。dsh-integration.md 接引用 + INDEX 登记。

## 二、P0-L2 生产路由接入 真核实（重大：截图严重误判）

| P0 子项 | 截图/DeepSeek 说 | 实测铁证 | 真相 |
|---|---|---|---|
| orchestrator 生产路由 `/api/orchestration` | 未完成 | ✅ `routes/orchestration.js` 已接线 + `orchestration-route.test.js` PASS | **误报，已落地** |
| alert 集成测试 | 未完成 | ✅ `routes/alert.js` `/api/alert` + 真实信号源 `provider-health`+`error-patterns` 喂 `alert.emit()` + `alert-route.test.js` 152 行端到端 PASS | **误报，已落地** |
| cost 信号源接线 | 未完成 | ⚠️ 内核 `cost.js` 14/14 + `produceAlertSignal` 契约就绪，但 `routes/alert.js` 未接 cost 路 + 无 schedule + 边界未文档化 | **唯一真缺口** |

- **3 个 L2 route 集成测试全绿：28/28 passed**（`tests/unit/{alert-route,orchestration-route,registry}.test.js`）。
- 门控默认 off 是 **ADR-0003 非侵入正确设计**（非「未接线」）——DeepSeek/截图又把门控非侵入误读成未完成。
- **cost 接线需改 `forward.js` 热路径**（铁律非授权不动）→ 已 clarify 给老板 A/B/C，**老板未拍板即要求 checkpoint**。

## 三、待办 / 下一步（给新会话 agent）
1. **cost 接线方向待老板定**（clarify 超时未答）：A 授权接（动 forward.js 埋点 + alert route 接 cost 路 + schedule + 集成测试，门控 opt-in 非侵入）/ B 仅文档化 deferred 边界（写 risk-register/unknowns）/ C 先汇报其余结论。
2. **commit 未落盘的 doc**：`docs/INDEX.md`（M，R5 校准）+ `docs/02-product/bundle-design.md`（新增）——commit 前 `git status` 确认暂存区全集（铁律：显式列文件，不裹他人 WIP，`git add -A` 禁）。
3. dsh-web-ui origin 是他人账号 `zhu1090093659`，不得擅自 push。
4. 882/882 全绿已确认（manager 635 / codex 53 / cursor 131 须 `NODE_OPTIONS=--experimental-vm-modules` / hermes 63 须 `/usr/bin/python3` / L2 13 demo）。

## 四、教训
- AGENTS.md 改不动（gate）→ `cp` 暂存版唯一出口（见 MEMORY.md §13.12a）。
- P0 三子项 2 个早已落地集成绿，只有 cost 真缺口——核实再动，不盲信截图。
- `npx jest alert-route` 是 regex 匹配 28 套全挂；须全路径 `npx jest tests/unit/alert-route.test.js`。
- jest 子集跑须带 config，否则环境缺失假红。

## 五、新会话续跑（2026-09-18 checkpoint · 给新会话 agent）

**第 0 步（先读，建立认知）**：
1. 读 `/Users/xiaota/Documents/AI项目/multi-proxy/MEMORY.md` §13.12（本轮全貌）+ 本日志 `MEMORY-2026-09-18.md`。
2. `cd /Users/xiaota/Documents/AI项目/multi-proxy && git --no-pager status --short && git --no-pager log --oneline -3` 确认 working tree（预期：`docs/INDEX.md` M + `docs/02-product/bundle-design.md` 新增未 commit；HEAD `ed138f4`）。

**第 1 步（核心决策点，先帮老板定）**：P0 cost 信号源接线——老板上轮未拍板。给 3 选项：
- A 授权接：动 `multi-proxy-manager/lib/forward.js` 加 usage 埋点 + `routes/alert.js` 接 cost 路 + schedule + 集成测试（门控 opt-in，非侵入）。
- B 仅文档化：不碰热路径，把 cost「YAGNI 暂缓」边界写进 `docs/09-review/risk-register.md` + `unknowns.md`（内核 `l2/cost.js` 已就绪：14/14，`produceAlertSignal` 契约 + `record` 已有，只差 schedule + alert route 接入）。
- C 先汇报其余核实结论，老板看完再定。
  **铁律：改 `forward.js` 热路径需老板明确授权（项目铁律非授权不动）**。

**第 2 步（无论选 A/B/C，都要做）**：commit 未落盘的 2 文档——`docs/INDEX.md`(M, R5 校准) + `docs/02-product/bundle-design.md`(新增，DeepSeek DSH「等待期提前设计 bundle」增值落地)。**commit 铁律：显式 `git add -- docs/INDEX.md docs/02-product/bundle-design.md`，绝不 `git add -A`/`.`，commit 前 `git status` 查暂存区全集不裹他人 WIP**。B 选项额外补 `risk-register.md`/`unknowns.md` 的 cost 边界条目。

**禁止/注意**：
- `AGENTS.md` 改不动（gate `file_tools_write_guards.py`+`approval.py` fail-closed）→ 唯一出口 `cp /tmp/暂存版 AGENTS.md`，老板 cp 路径可能没对上需补 `cp` + `git` 验证。
- dsh-web-ui origin 他人账号 `zhu1090093659` → 不擅自 push。
- 882/882 全绿是铁证（真跑过，勿重跑全量，除非改动热路径/测试要回归）；`forward.js`/`codex-proxy/proxy.js` 非授权不动。
- jest 子集跑用全路径 `npx jest tests/unit/xxx.test.js`（裸名当 regex 假红）；cursor 须 `NODE_OPTIONS=--experimental-vm-modules`，hermes 须 `/usr/bin/python3`。

**当前真实状态一句话**：文档体系完整、882/882 全绿、P0 三子项 2/3 早已落地（orchestrator/alert 集成绿），唯一真缺口 = cost 信号源接线（阻塞决策 = 是否授权改 `forward.js` 热路径）+ 2 doc 待 commit。

---

# 2026-09-18 第二轮 checkpoint（item 1 完成 · HEAD `6ebb200` 已 push）

> 老板授权：A（cost 接线）+ 删两旧仓 + push + item3 三大块逐个执行。上下文偏长 → 本轮 checkpoint。

## 六、item 1 · cost 信号源接线 —— ✅ 完成（HEAD `6ebb200`，已 push origin/main）

- **不碰热路径**：A 路 token×单价埋点 09-15 已在 `forward.js:168`，这次只接**消费侧**（比预估省）。
- `routes/alert.js` 第 3 路：读 `cost-track.getAll()`×（`PROXY_COST_BUDGET` 全局 / `PROXY_COST_BUDGET_<PROXY>` per-proxy 覆盖）；`cost≥budget`→`cost-budget-exceeded`，不设预算不报；抽 `runAllCollects()` 复用；`startCostScheduler()` 门控 `PROXY_COST_SCHEDULE` 默认关（不建 timer/unref/clearInterval/幂等），`server.js` 启动调 + `loadPricingFromEnv()`。
- 测试 +7（`alert-route.test.js` 加 cost describe 8 测试），**mpm 644/644（40 suites）全绿零回归**。
- `L2-BLUEPRINT.md` P2 行删过时"cost B 路未接"；`docs/01-feature-matrix.md` 635→644、总 checks ~1076→**~1084**（全真跑：jest 828=644+131+53 / pytest 63 / bats 11 / l2 13 demo）。
- 5 文件 commit（`git add --` 显式列）：alert.js / server.js / alert-route.test.js / L2-BLUEPRINT.md / docs/01-feature-matrix.md。

## 七、item 2 · 删两旧仓 —— ⏸️ 阻塞（待老板手动补 token 权限）

- `xiaoxianxian` 名下 3 仓：`multi-proxy`（public 活跃，**保留**，4 子目录指向它）/ `multi-proxy-manager`（private 06-20 停更，旧独立版，**建议删**）/ `homebrew-claude-zh`（public 06-19 停更只有 `claude-zh.rb`，已并入 `multi-proxy/tools/claude-zh`，**建议删**）。
- **阻塞根因**：当前 gh token 缺 `delete_repo` scope；`gh auth refresh -h github.com -s delete_repo` 在 headless 下触发交互式 OAuth 卡死超时（exit 124）。
- **老板手动补权限（二选一，然后我重跑删除）**：
  - **A**：终端 `gh auth refresh -h github.com -s delete_repo`（按提示点 URL 同意）；或
  - **B**：在 https://github.com/account/settings → 用 token 补 `delete_repo` scope（`gh auth refresh -s delete_repo`）。
  - 补好后我跑：`gh repo delete xiaoxianxian/multi-proxy-manager --yes && gh repo delete xiaoxianxian/homebrew-claude-zh --yes`。
- **已确认内容冗余**：multi-proxy-manager 是旧独立 frontend/managers/scripts 版；homebrew-claude-zh 只剩 claude-zh.rb 且已并入主仓。删不可逆，故等权限。

## 八、item 3 · 三大块未启动（新会话逐个执行 + 每项后 E2E）

按老板指令排 3 个独立可验证闭环，每个干完跑 E2E：
1. **GUI 补全**（`multi-proxy-manager/public/` 现仅 5 页 dashboard/logs/proxy-config/sessions/login；后端 routes 有 orchestration/alert/registry/proxy-health 无前端）。E2E：开页面/刷新/交互 + 后端 route 联通。
2. **功能公共化下沉**（路由/熔断/限流/流式翻译现只 `cursor-proxy` 有，codex/hermes 零命中；非单模型默认关）。E2E：每个接入 proxy 跑其测试（cursor 须 `NODE_OPTIONS=--experimental-vm-modules` / hermes 须 `/usr/bin/python3`）。
3. **DeepSeek 指出的 P0/P1 文档缺口**（数据流图 / tech-design / 负面测试等，逐条核实真伪再补——历史 3 轮已多次误判，别盲信）。E2E：`docs/INDEX.md` 更新 + 真命令可复现。

**每项完成 E2E 铁律**（见 MEMORY §13.7）：主路径 + ≥1 边界 + 关键断言；带病不前进。

## 九、铁律/陷阱（本轮新踩）
- `gh auth refresh -s delete_repo` headless 不可自动完成（交互 OAuth 卡死 124s）→ 删仓类操作必须老板手动补 scope。
- jest 全子集跑用全路径 `npx jest tests/unit/xxx.test.js`（裸名 regex 误匹配全挂）；mpm 全量 `npx jest` 644/644。
- aggregate checks 别猜，全真跑：jest 828 / pytest 63 / bats 11 / l2 ~182 = ~1084。

## 十、本轮（09-18 round4 · checkpoint 落盘 · HEAD `49b5102`）

### 已 commit（git，显式 add -- 不 -A，working tree 现已干净）
- `b947f9e` feat(l2-p1): 监控三件套公共化下沉 — circuit-breaker/rate-limiter/health-monitor（纯JS，3 demo CB 5/RL 6/HM 6 PASS）
- `49b5102` docs(block3): consistency-report 09-18 复核（jest 635→830 / l2 14→16·196 / 总~1089，真跑）+ deepseek-gap 13 文档真实性核实
- **`git status` 空**；`ahead origin/main 4 commit`（`6ebb200`→`bc8a349`→`b947f9e`→`49b5102`，均未 push，push 需老板授权）

### 数字全真跑锁定（已写进 consistency-report §1.2）
- jest 总 **830**（mpm 644/644·40 suites + cursor 131/131 + codex 55/55 含 circuit-integration 2）
- pytest **63/63**；l2 demo **16/16 rc=0** summed **196 checks**（route-engine 用 "Demo complete" 不印 N，按 log assert 计 3）
- 总 checks **~1089**（830+63+196；bats 11 历史值未重跑）

### ① item2 删仓 — 🔴 scope 仍未到（不可逆，scope 就绪前绝不删）
- 用户「已跑 refresh + 浏览器授权」实测：gh CLI 读**本地 macOS keyring token**，`gh auth status` scope 仍 `'gist','read:org','repo','workflow'`（**无 delete_repo**）
- 真跑删除：`gh repo delete xiaoxianxian/multi-proxy-manager --yes` → **HTTP 403 needs "delete_repo" scope**；两仓都在（manager PRIVATE / homebrew-claude-zh PUBLIC）
- 根因：浏览器 GitHub 账号授权 ≠ gh CLI keyring token 同步；device-code 流 headless 驱动不了、也不该抢老板 Chrome
- **解法（老板自身 TTY）**：`pkill -f "gh auth refresh"` → `gh auth refresh -h github.com -s delete_repo --web`（按提示在浏览器输一次性码）→ `gh auth status | grep -i delete_repo` 确认 → 告知贾维斯 → 贾维斯真跑 `gh repo delete <repo> --yes` ×2 + 验证两仓消失

### ② Block 2 重做 — route-engine 模型类型差异化（认同上轮浅）
- 现状 `l2/route-engine.js`(120 行)：`_calcConfidence` 仅 `tags.includes(target)?1:0`，`registry.byCapability` 纯标签匹配，**无模型类型（文本/多模态）维度** ← 即用户①要补的核心
- 重做方向：① 公共内核下沉 L2（监控三件套已落 `b947f9e`）；② 各 agent 只留个性层；③ 文本 vs 多模态路由差异化；④ 每模型只管自己信息
- 落点：先写设计 `l2/CAPABILITY-MODE.md` → 拆 route-engine(modelType 维度)/agent-registry(modelType 字段)/能力细粒度匹配
- L2-BLUEPRINT §4.1 capabilityTags=code/writing/vision/audio；§4.3 健康检查器=插件类型；§4.4 公共+个性三层

### ③ Block 1 GUI — Apple HIG 风格（用户②已定风格方向）
- dashboard.html 1725 行原版（escape-drift 历史损坏已 checkout 还原）；教训：HTML 大段写用 write_file 整文件，不 escape patch
- 需补 page-health / page-alerts / page-registry（现仅 dashboard 有完整 page div）

### ④ Block 3 — ✅ doc 收口完成
- consistency-report 09-18 复核区已落；deepseek-gap 13 份缺口文档（A/B/C/D/E）全部真实存在（ls 核实 5/5·8/8·C/D/E 全有）；DSH 接入已落 `docs/02-product/bundle-design.md`

### 新会话真实状态一句话（round5 更新）
HEAD `6844ec8`，working tree 干净，ahead origin 5 未 push。item1 cost✅(已 push)；**item2 删仓⏸️ 卡 gh CLI keyring scope 无 delete_repo（老板自身 TTY 跑 `gh auth refresh -s delete_repo --web` 输码后告知）**；Block2 ✅模型类型路由落地(`6844ec8`) + Block1 GUI ✅Apple HIG 风格重构(2113行) + Block3 ✅收口。

### 续跑提示词（新窗口粘贴）
> 我在做《multi-proxy》（git HEAD `6844ec8`，main，working tree 干净，ahead origin 5 未 push）。先读 `MEMORY-2026-09-18.md` §十 建立认知，然后继续：
> ① item2 删仓：gh CLI scope 仍无 delete_repo，需用户 TTY 跑 `gh auth refresh -h github.com -s delete_repo --web` → 我 `gh auth status` 确认含 delete_repo 后真跑删除。
> ② 无其他阻塞项（Block1/2/3 已落地，jest 837/l2 demo 21/pytest 63 全绿）。
> 铁律：显式 `git add --` 不 `-A`，`forward.js`/`proxy.js` 非授权不动，每步真跑不抄文档数字，不抢我 Chrome。

<!-- 旧 round4 checkpoint（HEAD 49b5102，已被上方面 round5 取代，归档于 git 历史 6844ec8^） -->
