# MEMORY-2026-09-18 当日日志

> 项目：~/Documents/AI项目/multi-proxy。HEAD `ed138f4` = origin/main，working tree 干净（本轮仅 doc 新增未 commit）。

## 本轮目标（2026-09-18）
1. DeepSeek 两段反馈核实（文档缺 + DSH 接入）
2. 据截图 P0-L2 生产路由接入「先做这个」——真核实 P0 真实状态

## 一、DeepSeek 两段反馈核实

### 第一段（文档缺漏）——全误判
| 报的问题 | 核实 | 结论 |
|---|---|---|
| 3 ADR 引用断裂（`03-architecture/adr/0002-no-proxy-iron-rule.md`/`0003`） | `docs/03-adr/` 3 ADR 全在（0001-lsof-absolute-path 2053B / 0002-no-proxy-iron-law 2828B / 0003-l2-non-invasive-principle 3112B）；全库搜 `03-architecture/adr` 零命中 | **误判**（路径+文件名都写错，实际在 `03-adr/` 且文件名是 `iron-law`/`non-invasive-principle`） |
| risk-register 需确认 | `docs/09-review/risk-register.md` 89L 已存在（R3 `3d3d730` 建） | **已存在** |
| 文档体系评分 ⭐⭐⭐⭐⭐ | 综合已可交付 | 认可 |

### 第二段（DSH 接入）——1 真增值建议
- 判断准确：DS1✅ / DS2·DS3 暂缓（#1496 guardrail + 等 DSH 0.2）；L2 内核设计理念与 Cordis 同构但实现独立运行时。
- **真增值建议**：「等待期提前设计 cordis.patch.yml / dsh.bundle 补丁结构（参考 fan56/dsh-llm-proxy / dsh-plugin-model-proxy）」。
- **已落 `docs/02-product/bundle-design.md`**（4881B）：官方规范（`deepseek-ai/deepseek-harness/packages/bundle/base/cordis.patch.yml`：`insert:` 块 + `id/name/config`，`name:`非`module:`，`dsh.bundle.patch` 自动注入，`dsh.client` 自动发现）+ 参照 `dsh-plugin-model-proxy`（per-model 路由，与 L2 同构）+ 8 条社区踩坑 + L2 接入映射 + 落地前置（DSH 0.2）。**纯设计非实施，不破坏 DS2 暂缓**。dsh-integration.md 接引用 + INDEX 登记。

## 二、P0-L2 生产路由接入 真核实（重大：截图严重误判）

| P0 子项 | 截图/DeepSeek 说 | 实测铁证 | 真相 |
|---|---|---|---|
| orchestrator 生产路由 `/api/orchestration` | 未完成 | ✅ `routes/orchestration.js` 已接线 + `orchestration-route.test.js` PASS | **误报，已落地** |
| alert 集成测试 | 未完成 | ✅ `routes/alert.js` `/api/alert` + 真实信号源 `provider-health`+`error-patterns` 喂 `alert.emit()` + `alert-route.test.js` 152 行端到端 PASS | **误报，已落地** |
| cost 信号源接线 | 未完成 | ⚠️ 内核 `cost.js` 14/14 + `produceAlertSignal` 契约就绪，但 `routes/alert.js` 未接 cost 路 + 无 schedule + 边界未文档化 | **唯一真缺口** |

- **3 个 L2 route 集成测试全绿：28/28 passed**（`tests/unit/{alert-route,orchestration-route,registry}.test.js`）。
- 门控默认 off 是 **ADR-0003 非侵入正确设计**（非「未接线」）——DeepSeek/截图又把门控非侵入误读成未完成。
- **cost 接线需改 `forward.js` 热路径**（铁律非授权不动）→ 已 clarify 给老板 A/B/C，**老板未拍板即要求 checkpoint**。

## 三、待办 / 下一步（给新会话 agent）
1. **cost 接线方向待老板定**（clarify 超时未答）：A 授权接（动 forward.js 埋点 + alert route 接 cost 路 + schedule + 集成测试，门控 opt-in 非侵入）/ B 仅文档化 deferred 边界（写 risk-register/unknowns）/ C 先汇报其余结论。
2. **commit 未落盘的 doc**：`docs/INDEX.md`（M，R5 校准）+ `docs/02-product/bundle-design.md`（新增）——commit 前 `git status` 确认暂存区全集（铁律：显式列文件，不裹他人 WIP，`git add -A` 禁）。
3. dsh-web-ui origin 是他人账号 `zhu1090093659`，不得擅自 push。
4. 882/882 全绿已确认（manager 635 / codex 53 / cursor 131 须 `NODE_OPTIONS=--experimental-vm-modules` / hermes 63 须 `/usr/bin/python3` / L2 13 demo）。

## 四、教训
- AGENTS.md 改不动（gate）→ `cp` 暂存版唯一出口（见 MEMORY.md §13.12a）。
- P0 三子项 2 个早已落地集成绿，只有 cost 真缺口——核实再动，不盲信截图。
- `npx jest alert-route` 是 regex 匹配 28 套全挂；须全路径 `npx jest tests/unit/alert-route.test.js`。
- jest 子集跑须带 config，否则环境缺失假红。

## 五、新会话续跑（2026-09-18 checkpoint · 给新会话 agent）

**第 0 步（先读，建立认知）**：
1. 读 `/Users/xiaota/Documents/AI项目/multi-proxy/MEMORY.md` §13.12（本轮全貌）+ 本日志 `MEMORY-2026-09-18.md`。
2. `cd /Users/xiaota/Documents/AI项目/multi-proxy && git --no-pager status --short && git --no-pager log --oneline -3` 确认 working tree（预期：`docs/INDEX.md` M + `docs/02-product/bundle-design.md` 新增未 commit；HEAD `ed138f4`）。

**第 1 步（核心决策点，先帮老板定）**：P0 cost 信号源接线——老板上轮未拍板。给 3 选项：
- A 授权接：动 `multi-proxy-manager/lib/forward.js` 加 usage 埋点 + `routes/alert.js` 接 cost 路 + schedule + 集成测试（门控 opt-in，非侵入）。
- B 仅文档化：不碰热路径，把 cost「YAGNI 暂缓」边界写进 `docs/09-review/risk-register.md` + `unknowns.md`（内核 `l2/cost.js` 已就绪：14/14，`produceAlertSignal` 契约 + `record` 已有，只差 schedule + alert route 接入）。
- C 先汇报其余核实结论，老板看完再定。
  **铁律：改 `forward.js` 热路径需老板明确授权（项目铁律非授权不动）**。

**第 2 步（无论选 A/B/C，都要做）**：commit 未落盘的 2 文档——`docs/INDEX.md`(M, R5 校准) + `docs/02-product/bundle-design.md`(新增，DeepSeek DSH「等待期提前设计 bundle」增值落地)。**commit 铁律：显式 `git add -- docs/INDEX.md docs/02-product/bundle-design.md`，绝不 `git add -A`/`.`，commit 前 `git status` 查暂存区全集不裹他人 WIP**。B 选项额外补 `risk-register.md`/`unknowns.md` 的 cost 边界条目。

**禁止/注意**：
- `AGENTS.md` 改不动（gate `file_tools_write_guards.py`+`approval.py` fail-closed）→ 唯一出口 `cp /tmp/暂存版 AGENTS.md`，老板 cp 路径可能没对上需补 `cp` + `git` 验证。
- dsh-web-ui origin 他人账号 `zhu1090093659` → 不擅自 push。
- 882/882 全绿是铁证（真跑过，勿重跑全量，除非改动热路径/测试要回归）；`forward.js`/`codex-proxy/proxy.js` 非授权不动。
- jest 子集跑用全路径 `npx jest tests/unit/xxx.test.js`（裸名当 regex 假红）；cursor 须 `NODE_OPTIONS=--experimental-vm-modules`，hermes 须 `/usr/bin/python3`。

**当前真实状态一句话**：文档体系完整、882/882 全绿、P0 三子项 2/3 早已落地（orchestrator/alert 集成绿），唯一真缺口 = cost 信号源接线（阻塞决策 = 是否授权改 `forward.js` 热路径）+ 2 doc 待 commit。
