# AgentGit 集成设计草案（2026-09-25）

> 配套文档：`AgentGit-评估-2026-09-22.md`（下称"09-22 评估"）。本草案在其基础上从"是否采用"推进到"如何集成进 multi-proxy"。
> 与 09-22 评估存在两处事实冲突，已在 §7 标为"待实测"，不推翻旧结论，以实测为准。

## 1. 背景：agent-git 是什么（已核实，2026-09-25）
- Rust CLI `agit`，MIT，v0.2.6（2026-09-01 建仓，pre-1.0）。
- 原生 resume：Claude Code / Codex / OpenCode / OpenClaw / **Hermes** / **WorkBuddy**；Cursor 只读导入。
- 本地存储 `~/.agit`（标准 git 仓库）；自带 Git/Git LFS，不碰系统 PATH / 全局 Git 配置（与项目 non-intrusion 原则一致，见 `docs/04-tech/non-intrusion.md`）。
- 命令：new / import / log / commit / push / share / run / resume / clone。
- 跨运行时接力（handoff）已验证，但跨格式可能丢信息（转换前提示损失）。
- 安全：低熵密钥过滤 + 仓库级密钥字典（可逆占位符）。

## 2. 在 multi-proxy L2 架构中的层次
multi-proxy 当前两层：
- **代理层（已建）**：codex-proxy(18790) / hermes-proxy(18793) / cursor-proxy(18794) + multi-proxy-manager(18792)。负责实时流量路由、成本、熔断。
- **会话层（本草案新增）**：agent-git 补的"底层 agent 运行时原生会话归档"。

关键澄清（修正此前"agent-git = M7 引擎"的说法）：
- M7 任务级 Session 与续跑 已在 multi-proxy 内部部分落地（`multi-proxy-manager/lib/session-store.js` + `session-keepalive.js`，MEMORY 记"含 M7 已闭环全绿"）。它管的是"proxy 托管任务的 checkpoint/续跑"。
- agent-git 管的是"底层 agent 运行时（WorkBuddy/Hermes/Codex/Cursor）本机会话文件"的版本化与交接。这一层 M7 的 proxy 级 store 并不拥有（proxy 看得见请求响应，看不见 agent 原生 store 与压缩）。
- 两者互补：M7 = 任务级续跑；agent-git = 全量会话资产归档 + 跨 agent 接力。集成后形成"任务续跑 + 会话资产"双层。

## 3. 目标与非目标
目标：
- G1 一键检测本机已装运行时（WorkBuddy/Hermes/Codex/Cursor/Claude Code），调用 `agit setup` 无感接入。
- G2 本地归档默认开启（commit 不上云），满足本地优先。
- G3 在 multi-proxy-manager 控制台聚合各 agent 会话 git 仓库，提供跨 agent 历史总览。
- G4 把跨运行时 `resume` 做成"把 A 会话丢给 B agent 接着干"的能力按钮。
- G5 密钥治理：GUI 暴露"仓库级密钥字典"配置，阻断裸 push 泄密。

非目标（v1）：
- 不自建 hub / 不接公共 hub 做分享（v1 本地归档足矣；分享留 v2）。
- 不改造 agent-git 核心（采用 bundle 或 fork 自持，见 §5）。
- 不做远程遥控（桌面端未实现，09-22 评估已证）。

## 4. 架构（文字视图）
```
[用户本机]
 ├─ WorkBuddy (GUI/CLI)  ── session JSONL ──┐
 ├─ Hermes agent         ── state.db     ──┤
 ├─ Codex / Cursor       ── JSONL        ──┼──→ agit adapters (只读原生 store)
 └─ multi-proxy-manager (18792)            │      │
        │                                  │      ├─ agit setup --runtime X --hooks --skill --mcp
        │  console: 会话仓库聚合 / 跨 agent 接力 │      └─ ~/.agit/ (git repos, 本地)
        │                                  │
        └─ M7 session-store.js (任务级 checkpoint/续跑, 已有)
```
- 数据流：agent 原生 store → agit 逐轮 hook 落盘 → ~/.agit git 仓库 → manager console 聚合 + 跨 agent resume。
- 与 M7 协同：在 M7 checkpoint 边界触发 `agit commit`，使"任务续跑态"与"全量会话快照"对齐。

## 5. fork 自持策略（规避年轻项目风险）
- 风险：建仓 1 个月，曾破坏性重写（旧 `@einsia/agentgit` 不兼容）。直接核心耦合有协议风险。
- 对策（MIT 允许）：
  - 方案 A（推荐 v1）：bundle 官方预编译二进制（npm 子包，按 os/cpu 分发），通过 `agit` 子进程调用，multi-proxy 只做编排层，不直接依赖其内部 API。版本钉死。
  - 方案 B（若需深度定制）：fork 仓库，自持有"本地 git store + 运行时适配器"部分（read/import/commit 逻辑），hub 部分可剥离。把"依赖"变"资产"。
- 不碰系统 PATH / 全局 Git：agit 自带运行时，满足 non-intrusion 原则。

## 6. 模块设计
### 6.1 运行时检测
- 复用 agit 的 adapter 发现（`agit doctor` 或 adapter registry），探测 ~/.hermes、WorkBuddy 项目 JSONL、Codex/Claude Code session 目录、Cursor 配置。
- 输出：已装运行时清单 + 各运行时 native resume / 只读导入能力标记。

### 6.2 一键接入
- manager 调用 `agit setup --runtime X --hooks --skill --mcp`（各运行时独立开关）。
- 处理 WorkBuddy GUI 捕获待实测 → fallback：`agit import` 手动收养历史会话。
- Hermes 原地压缩 → 提示用户关键节点手动 `agit commit`。

### 6.3 会话仓库聚合（console）
- 读取 ~/.agit 下各仓库，展示：运行时、最近 commit、会话时长、跨 agent 可接力标记。
- 操作：browse(log) / import / commit / resume。

### 6.4 跨 agent 接力
- `agit resume owner/repo@branch --as <target-runtime>` 封装为按钮。
- 跨格式损失前置提示（agit 原生行为），UI 展示"将丢失：X"。

### 6.5 密钥治理
- GUI 暴露 `agit secrets add` 等价配置（仓库级密钥字典）。
- push 按钮默认禁用，需显式开启并二次确认（防误泄）。

## 7. 待实测清单（与 09-22 评估的冲突点）
1. **离线 commit 行为**：09-22 评估称"0.2.3 下不登录 hub 无法 commit/resume，只能 --link-only"。我方 09-25 读当前 README 显示 commit 为本地动作、push 才上云。需在钉死的版本上实测 `agit import + commit + resume` 是否真能纯离线跑通。结果决定"本地优先"是否成立。（注：更广的竞品已二次求证，见 §11——Entire 已确认账号绑定、cass 已确认 MIT+rider 可用，不改变本项 P0 测试，但影响默认 provider 选型。）
2. **自托管 hub 可行性**：09-22 评估称 backend 仓库未公开（404），唯一能跑的 hub 是官方云。当前 setup 文档详述了 backend build/run（`AgentGit-backend`，127.0.0.1:8177，支持 `AGIT_HUB_URL` 指向自建）。需确认 backend 源码是否公开可拉。若不可得，分享功能（G 之外）只能接官方云或放弃。
3. **WorkBuddy GUI 自动捕获**：官方 mechanism 文档明说 GUI 任务列表注册未实现，CLI 驱动 hooks。需实测 GUI 会话是否被 hook 捕获，否则走 import fallback。
4. **Hermes 压缩下连续性**：官方称并发 rewind/compaction 未验证。长会话依赖主动 commit。

## 8. 风险与缓解
- 协议变动：版本钉死 + fork 自持（§5）。
- 供应商停滞：本地 store 是标准 git，即使 agit 停更也能自读自迁，不被锁死。
- 密钥泄漏：默认禁 push + 字典配置（§6.5）。
- 范围蔓延：v1 只做本地归档 + 控制台聚合 + 跨 agent 接力，分享/hub 留 v2。

## 9. 路线图
- P0（验证）：钉版本 → 实测 §7 四项 → 确认本地离线闭环成立。
- P1（接入）：运行时检测 + 一键 setup + 控制台会话仓库聚合。
- P2（增值）：跨 agent 接力按钮 + M7 checkpoint 边界触发 commit + 密钥治理 GUI。
- v2（可选）：自托管 hub / 分享链接（取决于 §7.2 结论）。

## 10. 参考
- `multi-proxy/AgentGit-评估-2026-09-22.md`
- `multi-proxy/MEMORY.md`（M7 session-store 现状、L2 蓝图）
- `multi-proxy/L2-BLUEPRINT.md`、`docs/04-tech/non-intrusion.md`
- 上游：github.com/Einsia/agent-git（README、`docs/mechanism-probing/openclaw-hermes-workbuddy.md`）
- **`multi-proxy/会话存档插件-竞品调研与选型-2026-09-25.md`**（竞品全貌 + `ConversationArchiveProvider` 接口草稿 + cass 存储路径地图）

## 11. 备选后端 / 竞品调研（2026-09-25 二次求证补充）

> 完整分析见 `会话存档插件-竞品调研与选型-2026-09-25.md`。结论先行：**agent-git 不是唯一解，应做插件化、可拔插，而非焊死。**

- **cass 协议真相**：字段虽显示 `NOASSERTION`，LICENSE 原文为 **MIT + OpenAI/Anthropic 排除 rider**——除这两家外任何人（含本项目）可用/改。**从「高风险待定」降为「可用，仅需注意 rider 边界」**，可直接作为检索后端（CassProvider）。
- **Entire 本地性修正**：`entire login` 浏览器登录 + token 派生云端控制面，**非纯离线**。故 **排除出「本地优先」默认**，仅当用户显式接受账号时才露 `EntireProvider`；不影响 G1–G5（agent-git 默认仍纯本地）。
- **re_gent 补录**：794★、Apache-2.0、Go；`rgt blame` 逐行 prompt 归因（provenance 最强），但**仅支持 Claude Code**——作可选 `ReGentProvider`，不进 v1 默认。
- **agenthist 补录**：120★、MIT、TS；A+B+C 混合 + 自带 Skill，可作跨 agent 转换/经验抽取参考。
- **选型结论（v1）**：默认 `AgentGitProvider`（纯本地 git + WB/Hermes 一等公民）；挂 `CassProvider` 做检索增强；战略路径 `SelfBuiltProvider` 复用 M7 `session-store.js` 叠 git-backend adapter（完全本地优先、协议零风险）。`ConversationArchiveProvider` 接口草稿已落新文件 §5，GUI 一键拔插即零成本替换。
