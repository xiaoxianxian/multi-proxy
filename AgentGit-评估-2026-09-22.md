# AgentGit 适用性评估（2026-09-22）

> 来源：用户分享的公众号文章《我天，居然有人在给 Agent 重新造 Git》+ 实测仓库/官网核验。
> 结论一句话：真项目、工程不水，但"抗打"要打折；对 multi-proxy 有潜在价值，但**只建议用本地模式**。

## 它是什么（已核验）
- Rust CLI `agit`，MIT 开源（CLI 部分），v0.2.2（2026-09-22 时仅发布约 3 周，pre-1.0，提交多为 fix，仍在抖）。
- 在 agent 的 session JSONL 文件之上做版本快照（这些文件平时会被覆盖/压缩/清理）。预编译包**捆绑 Git + Git LFS**。
- 原生 resume 支持：Claude Code / Codex / OpenCode / OpenClaw / **Hermes** / **WorkBuddy**；Cursor 仅只读导入。
- 本地存 `~/.agit/`；默认 push 到云端 hub `agent-git.com`（需 `agit login`）。

## 本地模式怎么用（数据不出本机）
- `agit import --link-only` 可离线"登记"一个本地 runtime 会话（仅关联、不抓历史）；`agit status` / `log` 浏览关联状态。
- ⚠️ **实测结论**：要真正带版本快照 / `commit` / `resume` 续跑，**必须 `agit login` 到 agent-git.com hub**——离线只能 `--link-only`，拿不到"会话持久化"这个核心价值。所以"纯本地不登录"在 AgentGit 0.2.3 下不成立。

## 对 multi-proxy 的相关性（重点看 M7）
- multi-proxy 的 **M7 任务级 Session 与续跑** 是 next priority（见 MEMORY.md）。AgentGit 正好是"会话快照/续跑"这一层能力的现成实现，**可能省掉自研持久层**。
- 但注意分层：multi-proxy 管的是"代理网关/路由"（Codex/Hermes/Cursor 三个 proxy 服务），AgentGit 管的是"agent 运行时落在本地的 session 文件"。两者不冲突，可互补：AgentGit 负责把各 agent 跑出来的上下文快照下来，multi-proxy 负责把它们编排流转。
- cc-switch 那条"无状态、不存消息体"的痛点，**AgentGit 救不了**——因为 cc-switch 本地根本不落 session JSONL；只有 underlying runtime（Claude Code / Codex / Hermes / WorkBuddy）落了文件的才抓得到。

## 三大坑（已核验，别被文章忽悠）
1. **"无损跨 Agent"是营销词**：同 runtime 内快照是真的；跨 runtime 官方明说"可能丢信息，转换前会提示损失"。
2. **密钥扫描不是自动拦截**：是"你先用 `agit secrets add` 注册、它再在边缘自动打码"的设备本地过滤器；没注册的/高熵 key 照样漏。FAQ 也让你自己审。
3. **云端优先 + RC 守护进程**：默认 push 到公共 hub；`agit rc` 能远程控你本机 Agent，是能力也是攻击面。本地优先党别开。

## 建议
- 先用本地模式在 WorkBuddy / Hermes 上试 `agit import` + `resume`，验证它真能抓到本机 session 且能续跑。
- 若要服务 M7，把它当"会话存储后端候选"评估，别现在就深度耦合（pre-1.0，API 会变）。
- 绝不 `agit push` 到 agent-git.com。**自托管 hub 可行性已核验（2026-09-22）**：backend 是独立仓库 `AgentGit-backend`，但 GitHub 上不公开——Einsia 公开仓库仅 8 个、均不含 backend；`Einsia/AgentGit-backend`、`Einsia/AgentGit` 两个候选均 404；全网检索也找不到公开 backend 源码（只搜到同名无关项目 jsoprych/agit、btucker/agentgit）。官方文档虽详述了 build/run/register 流程并支持 `AGIT_HUB_URL` 指向自建实例，但前提是你能拿到 backend 源码——目前拿不到。结论：**自托管从源码不可行，唯一能跑的 hub 是官方云 agent-git.com**。M7 若想借 AgentGit 做会话持久化 = 接受云依赖，与本地优先冲突，不采用。
