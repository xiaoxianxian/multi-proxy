# Proxy Rebuild 第三轮会话交接 — 评审 P0 清单清零（2026-08-25）

> 本文件是第三轮会话交接。前情见 HANDOVER-2026-08-24.md（第二轮）与
> review-2026-08-24/（9 角色评审）。

## 一、本轮完成（6 个 commit，全部已提交 main）

| commit | 内容 |
|--------|------|
| `8d0f201` | agent-proxy-switch hermes base_url 读取 bug（awk 限定顶层 model: 块，delegation/auxiliary 的同缩进空值不再误读）+ 可插拔架构评估报告 |
| `56e4acf` | fetch-models/test-connection 错误文案区分（401/403 认证失败 / 404 / 网络错误）；**意外修掉存量 bug**：normalizeModels 四处 `for (const i...)` 自增即抛 TypeError，fetch-models 成功路径此前从未工作过 |
| `9df9353` | P0-3 日志页过滤器失效（前端只认旧文本格式、后端是 JSON Lines → 改为优先 JSON 解析）+ P0-4 cursor admin-api 加认证（PROXY_AUTH_TOKEN + x-proxy-auth，与 codex/hermes 同模式）+ CORS 收紧（默认不挂 cors 中间件；注意 cors({}) 仍返回 ACAO:* 的陷阱）|
| `12ddadf` | P0-5 三页 esc() 补转义引号（拆属性注入 XSS 链；CSP unsafe-inline 未动，属 P2 重构）+ P0-7 LaunchAgent KeepAlive=true→false（一次性脚本会被无限重拉）+ launchd 日志 >10MB 轮转 |
| `7df3226` | P0-6 cursor 加密密钥锚定项目根（不再依赖 cwd，跨目录启动可解密）+ P0-8 providers.json 原子写 tmp+rename（codex/hermes 两处；双进程内存副本互写问题留 P2 单写者重构）|
| `31fc0c3` | P0-1 compose 移除 manager-jwt 文件路径卷挂载 + setPassword EISDIR 针对性提示；P0-2 hermes BIND_HOST 环境变量（容器 0.0.0.0 / 本机默认 127.0.0.1）；附带修 Dockerfile.manager 缺代理目录 COPY（Managed proxies: none）+ 删 /app 反模式卷挂载 |

**至此 review-2026-08-24 的 8 项 P0 全部修复。**

## 二、测试基线（本轮实测）

Codex 36 + Hermes 63 + Cursor **76**（+5 admin-api 认证测试）+ Manager **389**
（+10 错误文案分类测试）= **564/564 全绿**

跑 Cursor 测试：`PATH="$HOME/.local/bin:$PATH" NODE_OPTIONS=--experimental-vm-modules npx jest`
（better-sqlite3 已针对 v22 编译；用 v24 会报 ABI 137 不匹配）

## 三、Docker 实测记录（2026-08-25）

- 4 容器全部构建并启动成功，宿主机 curl 四个 /health 全 200
- 容器内首设密码成功（bcrypt 落盘 manager-state 卷）、无 token 启停 API 401
- Manager 正确列出 Managed proxies: codex, hermes, cursor 且探活全 healthy
- 测试密码已清理（manager-state 卷已删除）
- **容器当前已停止**（本机直跑服务为日常主力，已恢复运行）
- 坑：hermes/codex 的 `/app` 卷挂载会让旧卷内容盖住镜像新代码——已从
  compose 移除该反模式；如再遇「改了代码容器里没生效」先查这个

## 四、剩余待办（按优先级）

1. **评审 P1 清单**（review-2026-08-24/00-SUMMARY.md）：忘记密码指引路径错误、
   providers.json 明文权限、delete 后判空死代码 ×3、JSON 解析错误泄堆栈、
   proxy-config 暗色模式、history/switch-model/exportLogs 断头路、日志表索引
2. **P2**：单写者重构 providers.json 双写问题、server.js 拆分、CSP 去
   unsafe-inline（需内联 JS 外置）、数据库迁移框架/备份/日志轮转
3. **验收清理**：providers.json 里两条测试供应商（DeepSeek-Test/E2E）、
   cursor data/proxy.db 的 deepseek 测试数据、Docker 容器去留（现停着，
   可 `docker compose down` 并删卷彻底清理，或保留备用）
4. **fetch-models 参数名统一**（test-connection 用 model、fetch-models 用
   providerId，交接文档遗留项）

## 五、环境状态（2026-08-25 会话结束时）

- 本机直跑 4 服务运行中：codex:18790 / manager:18792 / hermes:18793 / cursor:18794
- Manager 密码：test-pass-12345（存 ~/.multi-proxy-manager/password）
- Docker：proxy-rebuild-* 4 容器已停止（未删除），manager-password 卷已删、
  manager-state 卷已删重建过（无密码残留），manager-jwt 卷仍在未引用
- cc-switch 15721 未受影响

---

# 📖 新会话开场白（直接复制）

```
读取 /Users/xiaota/proxy-rebuild/HANDOVER-2026-08-25.md 了解进展。
然后继续执行第四节待办清单。
每步完成后跑全量测试验证。
```
