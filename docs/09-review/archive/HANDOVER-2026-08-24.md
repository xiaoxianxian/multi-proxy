# Proxy Rebuild 第二轮会话汇报 — 主链路实测与修复（2026-08-24）

> 本文件是第二轮会话的完整交接文档。新会话开始时请先读本文件，
> 再按需读取 `review-2026-08-24/` 目录下的评审报告。

## 一、本轮完成的修复（3 个 commit，全部已提交 main 分支）

### commit `322be13` — 主链路三处关键修复
1. **Cursor settings 500**：旧 SQLite 库的 settings 表缺 `value_type` 列
   （新代码 INSERT 需要），PUT /admin-api/settings 必 500 → 路由模式切换失效。
   修复：`cursor-multi-model-proxy/src/db/database.ts` 新增 D2 迁移自动补列。
2. **Cursor UNKNOWN_PROVIDER**：provider_id='deepseek' 在合法类型列表里
   但从未注册适配器。修复：`src/providers/index.ts` 注册专用实例；
   `openai-compatible.ts` 构造函数支持自定义 id。
3. **代理开关停不掉**（用户反馈"开启/关闭 agent 的代理开关走不通"的核心根因）：
   外部启动（manage.sh nohup）的进程不在 Manager 的 proxyProcesses 追踪表里，
   stopProxy 的 PID 白名单为空 → 永远杀不掉。
   修复：`multi-proxy-manager/server.js` stopProxy 增加 cwd 校验回退——
   通过 lsof 的 cwd 字段验证端口占用进程的工作目录与 config.cwd 匹配后授权 kill。
   实测：codex/hermes/cursor 三者 stop/start 全部 ✅。

### commit `26869ef` — 模型切换主链路打通（设计初衷偏离修复）
4. **codex-proxy 断头路**：Web UI 创建的供应商存进 providers.json 后完全不参与
   对话路由（findProvider 只认硬编码 UPSTREAM_MODELS + 环境变量 key）。
   修复：findProvider 优先匹配 providers.json 中启用的用户供应商；
   /v1/models 合并用户供应商 models；POST/PUT providers 支持 models 数组。
5. **codex 切模型静默失败**：UI 发 PUT /api/settings 但 codex-proxy 无此端点
   （404 被 fetchAdminApi 吞掉，UI 还显示成功）→ 新增 GET/PUT /api/settings
   （持久化到 ~/.multi-proxy-manager/settings.json）；manager 白名单放行。
6. **前端 switchModel 盲发**：对所有 proxy 都发 cursor 的 /settings 端点。
   修复：proxy-config.html 按 proxy 分流——hermes 走专用 POST /switch-model
   （重写 config.yaml），codex/cursor 走 PUT /settings。

### 回归测试
Codex 36 + Hermes 63 + Cursor 71 + Manager 379 = **549/549 全绿**
- 注意：跑 Cursor 测试需 PATH 用 `/usr/local/bin:$PATH`（Node v24, ABI 137）；
  本地运行服务用默认 node v22 —— better-sqlite3 已针对 v22 重编译（8月24日）。

## 二、主链路实测结论（回应"卸载前主链路走不通"）

| 链路 | 实测结果 |
|------|---------|
| 创建供应商 | ✅ 修复后可用 |
| 拉取模型列表 | ⚠️ 功能正常；但上游 401/403 时报「无法连接到供应商API」误导用户 |
| 测试连通性 | ⚠️ 同上；且参数名与 fetch-models 不一致（待统一）|
| 切换模型 | ✅ 修复后可用（此前 codex/hermes 均静默失败）|
| 代理开关 | ✅ 修复后可用 |

遗留体验问题：fetch-models/test-connection 应区分「认证失败(401/403)」与
「网络不通」两种错误文案。

## 三、设计初衷验证结论

| 初衷 | 结论 |
|------|------|
| 非侵入式 | ✅ 符合。只改 agent 自己的配置文件（config.toml/config.yaml），不动 agent 代码 |
| 切换实时生效 | ⚠️ 部分偏离。hermes-proxy 每请求重读 config.yaml 没问题，但 Hermes Agent 只在启动时读一次 config.yaml，切模型后必须重启 agent（API 已返回 needRestart:true）。架构性限制 |
| 切模型后已有会话继续用新模型+保留上下文 | ❌ 当前做不到。会话历史在 agent 进程内存里，重启即丢。需要 agent 侧支持（如 session 持久化），应在文档中明确此边界 |

## 四、cc-switch 并存方案（用户需求）

- 端口无冲突：cc-switch 15721 vs proxy-rebuild 18790/18792/18793/18794
- `tools/agent-proxy-switch` 就是为「单一所有者、两套工具并存、每次只开一个」设计的
- 新版 codex（ChatGPT 整合后）的 ~/.codex/config.toml 结构未变，
  仍是 `[model_providers.custom]`，切换工具识别 cc-switch 所有者正常 ✅
- 新字段 `model_catalog_json = "cc-switch-model-catalog.json"` 存在，
  切换时需确认不影响（未验证）

## 五、下个会话待办（按优先级）

1. **修 agent-proxy-switch hermes bug**（已定位根因）：
   `current_hermes_url()`（tools/agent-proxy-switch:75）用
   `grep -E '^  base_url:' | head -1` 取第一个 2 空格缩进的 base_url，
   但新版 ~/.hermes/config.yaml 里 `auxiliary.approval.base_url: ''`
   排在 model.base_url 前面 → 读到空值 → 所有者误判 unknown。
   修法：用 awk 限定在顶层 `model:` 块内取 base_url。
2. **可插拔架构改造评估**：当前 install.sh 全量安装，需支持单代理独立装卸；
   扩展新代理（Pi/Qoder 等）只需注册 PROXY_CONFIGS + FORWARD_ENDPOINTS
   （server.js 两处）+ manage.sh/install.sh 各加一段，建议抽成 per-proxy 配置文件。
3. **fetch-models/test-connection 错误文案区分** 401/403 与网络错误。
4. **多角色评审 P0 清单**（见 review-2026-08-24/00-SUMMARY.md）：Docker 密码目录、
   hermes 容器绑 127.0.0.1、日志页解析格式不匹配、cursor admin-api 零认证等 8 项。
5. **验收后清理**：删除测试密码 `~/.multi-proxy-manager/password`
   （当前测试密码 test-pass-12345）；决定 Docker 容器去留。

## 六、当前环境状态（2026-08-24 会话结束时）

- 4 服务本地运行中（非 Docker）：codex:18790 / manager:18792 / hermes:18793 / cursor:18794
- Manager 测试密码：test-pass-12345（存 ~/.multi-proxy-manager/password）
- cc-switch 正常运行于 15721（未被影响）
- 旧 Docker 容器（proxy-rebuild-* 4 个）已停止、未删除
- providers.json 里有两条测试供应商数据（DeepSeek-Test / DeepSeek-E2E，fake key）
- cursor data/proxy.db 有一条测试供应商（deepseek, fake key）

---

# 📖 新会话如何完整读取这些进展

## 方式一：让新会话的 agent 自己读（推荐）

开新会话后第一句话直接说：

```
读取以下文件了解项目进展和待办，然后继续执行第五节的待办清单：
1. /Users/xiaota/proxy-rebuild/HANDOVER-2026-08-24.md（本文件，交接总纲）
2. /Users/xiaota/proxy-rebuild/review-2026-08-24/00-SUMMARY.md（9角色评审汇总+P0清单）
```

agent 会用 read_file 工具读到全部上下文。各角色完整评审报告在同目录
`1-产品经理.md` ~ `9-普通用户.md`，按需再读。

## 方式二：利用项目记忆文件（长期机制）

项目的 CLAUDE.md 是每次会话自动加载的。可以把关键状态写进去，
但 CLAUDE.md 适合放「长期不变的规范」，不适合放进度类信息——
进度请以 HANDOVER 文件为准，CLAUDE.md 只需加一行指引：

```markdown
## 当前进度
最新交接文档见 HANDOVER-2026-08-24.md，评审报告见 review-2026-08-24/
```

（本轮已由 agent 完成 git 提交，HANDOVER 文件本身也在仓库里，
git log --oneline -10 可看到全部三个修复 commit。）

## 方式三：Hermes 会话检索

如果在新会话中想查历史对话细节，可以对 agent 说：
「搜索之前的会话记录里关于 proxy-rebuild 主链路修复的内容」，
Hermes 的 session_search 工具可以找回当时的对话。

## 建议的新会话开场白（直接复制）

```
读取 /Users/xiaota/proxy-rebuild/HANDOVER-2026-08-24.md 和
/Users/xiaota/proxy-rebuild/review-2026-08-24/00-SUMMARY.md 了解进展。
然后按优先级执行第五节待办：先修 tools/agent-proxy-switch 的
hermes base_url 读取 bug，再做可插拔架构评估。
每步完成后跑全量测试验证。
```
