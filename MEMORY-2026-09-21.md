# 2026-09-21 multi-proxy 进展日志

> 本轮会话核心：(1) fix pre-existing flaky (task2)；(2) 全项目盘点。
> 上下文已压缩 1 次 + 高消耗，本日志供新会话无缝接手。
> 基准 git：`HEAD = origin/main = 39f57bb`（origin 平齐）。

---

## 一、本轮完成：根治 pre-existing flaky（task2）✅ 已 push

**commit `39f57bb`**（`bfb57f0..39f57bb`，已 push origin，本地平齐）

### 问题
全量 `npx jest`（719 用例 / 45 suites）偶发挂 1 个，约 8–13% 频率。3 个文件碰**真实 tmux daemon**，多 worker 并发踩同一 daemon + 单一 state 文件跨 worker 串味 → flaky。

### 修法（hermetic，不碰真 daemon）
- 新增 `tests/unit/helpers/fake-tmux.js`（~100 行）：纯 node 假 tmux binary，实现
  `new/has-session/kill-session/list-sessions/attach-session` 子命令语义；state 落
  `JEST_WORKER_ID` 命名的 per-worker 文件 `fakemux-wid-<WID>-state.json`。
- 三套测试注入 `TmuxKeepalive({ tmux: FAKE_TMUX })` + `beforeEach` 清 fake state：
  - `session-keepalive.test.js`
  - `tmux-keepalive.test.js`
  - `p3d-crash-recovery.test.js`
- 真 tmux 仅保留两类与 daemon 无关的用例：`tmuxAvailable()` 可用性探测 + 注入不存在命令降级。

### 根因（已实证，非臆测）
- flaky 只在**全量并发多 worker** 出现；3 文件单独 `--runInBand` 各 30 次**全绿**。
- jest worker 内**运行时改的 `process.env` 不传给 `spawn` 子进程**（实测子进程 `PROBE_X=UNSET`），
  但 `JEST_WORKER_ID` 是 OS 级 env，**能传给 spawn 子进程且 worker 内稳定唯一**（实测 `CHILD_SEES=1`）
  → 用它做 per-worker state 命名空间，根治跨 worker 串味。

### 真跑数字
| 验证 | 结果 |
|---|---|
| 3 文件单独 `--runInBand ×30` | **0/30 fail ✅** |
| 全量并发（15×） | **14/15 全绿**，1 fail = `sessions-api GET /api/sessions` pre-existing 噪声 |
| 全量单次 | **719/719 / 45 suites / 0 fail ✅** |

### 残余观察（非阻塞，不扩大战场）
- `sessions-api.test.js › GET /api/sessions returns empty list` 并发全量偶挂 ~1–2/15，单跑 20/20 全绿
  → jest worker 复用 module-level `mockStore` 噪声，与 tmux 无关。可单独排期（jest testIsolation /
  改 `jest.mock` factory 缓存）。
- 本轮还观察到 `memory-merge-route`（1/10→1/30）+ `error-classification` 偶挂，均极低频并发噪声、
  非本任务引入、非稳定 bug。

---

## 二、全项目盘点（子代理 1931s 跑完，5 源 + 4 交叉核对，已核实）

> 测试基线：**719/719 (jest) / 17/17 (pytest) / ~1092 checks 全绿**；origin = 本地 = `39f57bb`。
> 热路径 `forward.js` / `codex-proxy/proxy.js` 全程零触碰；所有新功能默认门控关 = observe/shadow。

### ✅ 已交付 + 验证
- M1–M5 + M3 全完成；M6 引擎+shadow 完成（接真实路由 gated D4–D6）；M7 原语（worktree/tmux/crash）落地
- task1 省钱网关一期（`l2/savings-gateway/`，`bfb57f0`）：demo 24/24、jest 18/18、热路径 0 触碰、live 冒烟 18795 通过
- option1（complexity→tier 路由，jest 14/14 + 3 seed）
- option2 Octop harness-agent failover（**实现 17/17 pytest 真验**，`l2/python-middleware/`）
- option3 MCP bridge + 编排真执行（jest 26/26 + E2E 9/9；L 拆解真接线 `mcp-orchestrate-llm.demo` 12/12）
- P5 基础 GUI 桌面壳（Electron + 6/7 dashboard 页，已合并）
- 登录 bypass 事故恢复；manager 127.0.0.1 绑定（`580c640`，实证 `lsof` 非 0.0.0.0）
- flaky 根治（`39f57bb`）

### 🔴 3 处订正（与旧上下文/转述有出入，以文件真实内容为准）
1. **R2「uninstall 卸载不完整」其实已修**（2026-09-19，`a9d898f`，`check-uninstall.sh 25/25 PASS`，
   `risk-register.md` 标"已修(09-19)"）→ **当前项目无未关闭 P1 待修**。C2（`forward.js` axios 全缓冲）/
   B7（`proxy.js` 120s pipeTo 掐断长流）为"触发时 latent"，修了反坑，不修。
2. git HEAD 实际 `39f57bb`（含 `580c640` 绑定 + `bfb57f0` task1 + `39f57bb` flaky），
   docs 多处仍引用更早 `063a80e`/`765434e`/`640e12b`/`b69fc77` → **文档快照滞后，非阻塞**。
3. `feature-matrix` 主表头仍写 09-16 `b69fc77`，09-21 增补行已记 719/719（主表头是历史引用，增补行才是实时）。

### ⏳ 外部阻塞（不可自行推进，需外部依赖就绪）
| 项 | 卡什么 | 何时可做 |
|---|---|---|
| DSH DS2（发 npm bundle，`dsh.bundle` 字段） | 等 DSH **0.2** 稳定 + `dsh plugin add` P0 guardrail **#1496 未修** | 二者就绪后 |
| DSH DS3（用户实际 `dsh plugin add` 运行） | 同 #1496 | 同 |
| option2 harness-agent 发版（Cordis/DSH 包） | 等 DSH 0.2 + `deepagents` 包 **PyPI 404** | DSH 0.2 后 |
| task3 P5 Marvis 增强 7 类看板（设计稿 v2 已落盘 `docs/04-business/marvis-p5-gui-design.md` 257 行，未实施） | 老板定"等 dsh 生态版做完"，**排期决策非硬外部** | 老板拍板做不做 + 做到哪期 |

> 关键认知：**"抢占 DSH 生态" ≠ "发 DSH plugin 包"** — agentgateway / 省钱网关形态（OpenAI 兼容）
> **不卡 DSH 0.2，今天可动**；只有 DS2/DS3 卡 DSH 0.2 / #1496。

### 🙂 内部可做但卡人工 sign-off（高后果 / 改真实路由 / 写真实 DB）
| 项 | 现状 | 需老板拍板 |
|---|---|---|
| D6-b 健康决定真实路由（M6 接真实路由） | D6-a 已做（shadow 观测）；D6-b 卡 D4 语义（override vs failover-only，老板 09-13 选 C=override，Step3 翻 1 属人类决策）+ D5（enable 原 disabled 的 `qwen3.8:27b-mlx` ollama provider 写真实 DB） | **是**（误禁用 provider 重排全局路由 = 最高后果） |
| M2 自动隔离（`PROXY_HEALTH_ISOLATE`） | observe-only 默认关（`bb45837`） | **是**（独立 sign-off + live 回归） |
| 安全缺口 `log_redaction` / `routing_overrides` | `app.listen` 0.0.0.0 已修；这两个仍缺口 | **是** |
| token×单价埋点接 `forward.js` 热路径 | YAGNI，需 model→pricing 映射 + 单独门控 + 改热路径 | **是**（改热路径需授权） |

### YAGNI 二期（已诚实标注，不阻塞）
真执行（shadow off + executor 真打上游）/ 价格进 route 排序 / 省钱排行榜落盘 /
option3 HTTP/SSE 二期 + LLM 拆解真接线 / option1 动态 LLM 复判 — 各自独立增量。

> 当前 shadow / observe 态已是**稳态终点，非中途**；翻 override / 隔离 / 执行态均需显式开启 + live 回归。

---

## 三、待老板拍板的事（本轮未自行推进）
1. **§13.21 + 今日日志收口**：✅ **已 commit + push**（老板 2026-09-21 授权「执行commit/push 命令」）。
   命令：`git add -- MEMORY.md MEMORY-2026-09-21.md && git commit && git push origin main`。
2. **task3 P5**：做不做 + 做到哪期（P5.0→5.2）。设计稿 v2 已就绪。
3. **DSH DS2/DS3 / option2 发版**：等 DSH 0.2 + #1496，当前不可推进。
4. **D6-b / M2 / 安全缺口 / token×单价**：均需老板 sign-off，当前不可自行翻执行态。
5. **`sessions-api` 残余 flaky**：是否单独排期修（jest testIsolation / jest.mock factory 缓存）。

## 四、下一步建议
- ✅ §13.21 + 今日日志已 commit + push（2026-09-21，老板授权）、origin 平齐、工作区干净。
- 其余多为卡人工拍板 / 卡外部依赖，等老板指示；无可自行推进的开发项。
- 残余 flaky 若要根治：优先修 `sessions-api` 的 `jest.mock` factory 缓存 / 加 `testIsolation`。
