# MEMORY-2026-09-19 · checkpoint（option3 全落地 + 三份外部评估处理）

## 一、本会话完成的闭环

### option3 三阻塞全解（commit 007751e，已 push）
- **B1**：`route-engine.js` 加可注入 `executor` 槽（默认 null = 保持 `queued` 桩，零行为变）+ `runDag(dag, runOpts)` 委托 `Orchestrator.run`（路由阶段强制 shadow 避双重执行）。`route-engine-orchestrator.demo.js` 13/13；原 14/14 jest 不变量保持。
- **B2**：net-new MCP bridge — `l2/mcp-server.js`（JSON-RPC 2.0 over stdio，零依赖，门控 `PROXY_L2_MCP` 默认 0）+ demo 11/11 + `mcp-server.test.js` 13/13。
- **B3**：`routes/skill-service.js`（门控 `PROXY_L2_SKILL`）+ `routes/memory-merge.js`（门控 `PROXY_L2_MEMORY`）+ server.js 挂载 + 2 route test 12/12。

### MCP 接进 manager 面板（commit 6e4fcb6，已 push）
- `routes/mcp.js`（镜像 alert.js 门控模式，`PROXY_L2_MCP` 默认 0）+ server.js 挂载 + `mcp-route.test.js` 12/12。
- `PROXY_L2_MCP=1 PORT=18800 node server.js` 真跑探活：`/api/mcp/health` ok、`/api/mcp` toolCount=3、`/api/mcp/call` video→h3web shadow、未知 tool 422 -32602、`/api/mcp/rpc` ping 200。

### option3 收口 batch（commit 7d511b2，已 push）
- 处理本会话 3 份外部评估（见下）。

## 二、3 份外部评估的核实与处理结论

### 文件 1（DeepSeek A — 文档体系核查）
- 核心断言「INDEX 声明无实质缺口基本属实」→ 核实成立。
- 它点的 5 个「疑缺」逐项核实：**2 误判**（`commercialization-decided.md`/`m6-action-checklist.md` 已在 INDEX 登记 line 81/80/146）+ **2 低优先**（`design-tokens.md` 未独立、`04-business/` 内容待确认）+ **1 误报**（`04-business/.gitkeep` 不存在但目录非空，无需 .gitkeep）。
- 第 1 优先「以 INDEX 为入口做验收演练」（读 INDEX+feature-matrix 评完成度，不翻全仓）→ **本轮已隐式执行**（INDEX 能覆盖，无需全仓 grep）→ 采纳验证通过。
- 第 3 优先「AGENTS §7 文档同步」→ AGENTS **已有该条**（§7 line 93）→ 不重复加。

### 文件 2（DeepSeek B — DSH 生态接入）
- 核实 `bundle-design.md`/`commercialization-decided.md`/`commercialization-questions.md`/`dsh-integration.md` 全在；但 `package.json` 无 `dsh.bundle` 声明 + 无 `cordis.patch.yml` → **确认 DS2 仍暂缓**（DSH 0.2 + #1496 guardrail，外部条件，非代码缺口）。
- 战略建议（npm 占位 / DSH 社区预告 / skill-only 优先收录 / 模块映射表 / 完整 cordis.patch.yml 草稿 / DSH 0.2 跟踪清单）→ 非代码缺陷，登记待办，DS2 窗口内按优先级执行。

### 文件 3（WorkBuddy — 代码复核，最有价值）
- 复核确认 3 块真落地（`_mapTier` 4 端测、option2 中间件 17 真测、MCP server 标准 JSON-RPC）。
- **唯一实 bug**：`routeTask` complexity enum 声明 `trivial/complex` 但 `_mapTier` 只认 low/high → **已修**（收敛 `low/medium/high`，全仓 grep 确认无 caller 使用，零副作用）。
- 两点待办：① decomposer/orchestrator 未包成 MCP 工具 → P3 后续；② complexity 生产化需注册 small/med/large 3 profile → 部署配置。

## 三、自纠的一处放置错误
- `007751e` 把 ADR 放成根目录 `03-adr/ADR-002-mcp-bridge.md`，但正式 ADR 体系在 `docs/03-adr/`（0001/0002/0003）→ 位置不规范 + 002 撞号。
- 09-19 收口 batch 已 `git mv` → `docs/03-adr/0004-mcp-bridge.md` + 清理 stray 根 `03-adr/` + 修 4 处 ADR-002 代码注释引用 + INDEX 03-adr 栏 3→4 + 变更日志行。

## 四、当前 git 状态
- `origin/main` = 本地 HEAD = `7d511b2`。3 commit 已 push：`007751e`（option3 主体）+ `6e4fcb6`（MCP 接 manager）+ `7d511b2`（收口 batch）。
- working tree clean。热路径 `forward.js` / `codex-proxy/proxy.js` 全程 0 触碰。
- mpm 全量 **671 pass / 14 fail**（14 全 pre-existing：frontend/dashboard + frontend/design-system，0 新增回归）。

## 五、遗留/待办（非本会话引入，已登记）
- DSH DS2 实施（等 DSH 0.2 + #1496 guardrail）→ 战略窗口项。
- MCP 暴露 decomposer/orchestrator（P3c/P3d，需 executor 接真实 adapter 执行）→ 后续。
- complexity 生产化（3 profile）→ 部署配置。
- pre-existing 14 reds（frontend 套件）：与本批无关，未动。

## 六、本会话关键决策与铁律遵守
- 门控铁律：3 个新路由门控全部默认关（PROXY_L2_MCP/SKILL/MEMORY=0），零 kernel 改动、零回归。
- 显式 `git add --`（非 -A）；热路径非授权不动。
- 三份评估不盲从：实 bug 才改（enum），其余核实后登记，不写投机代码。
- 撞 `.git/index.lock`（stale，16:32 遗留 49min，无 git 进程持有）→ 确认后 rm 安全清。

## 七、本会话续（R2 收口 + check.sh 同款 CJK-safe，实跑验证）

### R2 修复（risk-register P1「install.sh --uninstall 卸载不完整」）——已 push `a9d898f`
- `install.sh`：新增 `plist_label`/`label_is_orphan`/`label_is_suspect`/`uninstall_launchd` 四函数
   - 家族白名单：含 `multi-proxy` 自动清（覆盖历史 `com.codex.*`/`com.xiaoxian.*` 残留）
   - `com.apple.*` 绝不删；跨家族（codex/hermes/cursor/xiaoxian）仅检测不自动删；坏 plist 跳过
   - 新增 `--uninstall --dry-run` 预览 + `LAUNCHD_DIR` 注入（隔离，不碰真实 ~/Library/LaunchAgents）
   - `set -e` 兜底（L281 `|| label=""`）：原版遇坏 plist 中断整个卸载（实跑揪出）
   - `BASH_SOURCE[0]` 守卫（L726）：可被 source 测试、不误触在跑代理
- 回归 `tests/shell/check-uninstall.sh` 25/25 PASS（7 类）
- `docs/09-review/risk-register.md`：R2 待修→**已修(09-19)**、§六 仅R2→无、last_updated→09-19

### 两个项目级 bug（实跑揪出，非臆造）
- **CJK 路径 bug**：`$SCRIPT_DIR/..` 字面拼接在含 CJK 目录（`.../AI项目/...`）下 `test -f` 返回
  MISSING → 整脚本崩溃。连预存 `tests/shell/check.sh` 都坏。修：改 `git rev-parse --show-toplevel`
  （无 `..` 拼接，CJK-safe）+ `$PWD` 回退。
- **bash 3.2 `declare -A` bug**：`check.sh:74` 在 macOS 系统 `/bin/bash` 3.2 报 `declare: -A: invalid`
  硬退出；变量仅声明 0 引用（死代码），删后 15/15 PASS。

### check.sh 修复 commit `f6dd1ab`（**未 push，待老板点头**）
- 下次 push：`git push origin main`（`a9d898f..f6dd1ab`）

### 本轮测试基线（实跑）
- mpm jest 683 pass / 14 pre-existing fail（0 新增回归）
- l2 demo 15/15；shell 回归 check.sh 15/15 + check-uninstall.sh 25/25

### 上下文健康
- 本轮末评估≈36% window + 已 1 次压缩 → 黄区，已存档；下一步计划建议开新窗口。
