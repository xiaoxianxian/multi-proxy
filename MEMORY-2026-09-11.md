# 2026-09-11 会话日志

## 战略转向（L1 → L2）

老板拍板：proxy-rebuild 从"多模型代理"重定位为 **Agent 编排中枢 / 网关+中台**
（对标 DeepSeek Harness "一切皆插件"）。

**四条设计原则（老板定）**：
1. 网关+中台分层（把 agent 比互联网公司各系统，项目=网关+中台）
2. 一切皆插件、热插拔（类 DeepSeek Harness）
3. 公共+个性记忆复用（各项目独立 + 跨项目共享 + 记忆注入）
4. 非侵入（尽量不侵入 agent 自身代码/文件，模型代理式拦截转发）

**②④ 冲突解法（老板确认）**：热插拔=项目自身模块；非侵入=不碰 agent 外部文件；
个性化注入走环境变量（`AGENT_SYSTEM_PROMPT` 等），不写 `~/.codex`/`~/.hermes`/`~/.cursor`。

## 产出文件（已 commit 本地 main · 已 push 同步 origin）

| 文件 | commit | 内容 |
|---|---|---|
| L2-BLUEPRINT.md | 24e57eb→d88f2ad | L2 产品+技术蓝图 v2（390 行，8 节） |
| architecture-v2.html | 29b0f0e | L1→L2 演进架构图（深色 SVG，可浏览器打开） |
| 文档漂移修复 | 8db3c9e | 测试数字 151/571/630→646 + M6 状态订正 |

## 蓝图 v2 新增/整合（v2 相对 v1）

- **4.8 Agent Adapter 协议（核心卖点·新增 P0）**：定义"任意 agent 怎么接入/卸载"的规范契约。
  任务接收(HTTP/MCP/文件队列) / 结果回传 / 能力声明(注册能力标签) / 健康检查。
  已覆盖形态：Codex/Cursor(MCP零改造) · Hermes(MCP+HTTP) · h3web/AIGC(HTTP,本地 Web 服务天然适配) · 用户自研(实现协议)。
- **6.1 Agent 通信协议 ✓ 已定**：分层——主流用 MCP(B 零改造)，本地 Web 服务用 HTTP(C 原生)，通用兜底文件队列(A)。
- **6.3 记忆注入 ✓ 已定**：环境变量(记忆/skill，零改造) + 运行时 hook/API(任务编排)。配置覆盖淘汰(违反④)。
- **6.4 插件安全+互通性(扩展)**：
  - 互通性(补充 2.1)：记忆/skill 方案可替换(x→y)，靠"规范数据格式 + 方案适配器 + 无损转换"。
  - 非业务插件(补充 2.2)：基础设施能力(成本优化/监控/日志)也是插件，如 zg 项目(减少 token 消耗)，用户可热插拔删/换。
- **Marvis GUI 降级为 P5 可选(锦上添花)**，核心 P0-P3 不变。
- **实施路径**：核心 P0-P3 约 4-5 周；GUI 可延后或不做。

## 三决策已拍板（2026-09-11 · 老板确认，按推荐执行）

1. **P0 模块顺序**：调整为 **Agent Registry → 插件运行时 → Adapter 协议**（原序把 Registry 排最后，但它其实是 Adapter 的前置、且工作量最轻，先做能最快出可验证 CRUD 当地基）。
2. **记忆/skill 规范格式**：**JSON 为 canonical 存储 + YAML 为人类可读视图（双向无损）+ `specVersion` 字段**。拒绝纯 YAML（机器存储的锚点/多行/类型推断在 x↔y 互换时是互通性噩梦）、纯 JSON（缺人类可读）。
   - 记忆：`{id, type, scope, key, value, tags[], source, createdAt, updatedAt, specVersion}`
   - skill：`{id, name, trigger, description, content, version, tags[], enabled, source, specVersion}`
3. **先做 1 个 adapter 端到端样例**：**做，选 h3web**（不选 AIGC）。
   - 选 h3web 理由：①最硬 case（仅 HTTP、无 MCP、无任务队列），打通即覆盖 Codex/Cursor 的 MCP 简单情形；②已在跑(8732)能真跑通；③**老板补充**——h3web 规划相对清晰，AIGC(画境 hujing-ai) 产品/规划仍在变化未定型，不宜锁 adapter。
   - 样例边界：P0 只做**接口契约**（能力声明 + /health + 任务提交/结果回传 + 1 真实视频 round-trip），**不碰编排/shadow**（属 P2）。铁律④：不写 h3web 任何文件、不改其端口配置（端口动态探测，非写死 8732）。

## 三项目优先级

- proxy-rebuild → L2 主力（本蓝图）
- 绘境 AI → 独立推进，节奏放慢，不额外投入
- content-audit → 维持 P2，不额外投入

## 现状

- 基线 646/646 全绿（manager 436 + cursor 111 + codex 36 + hermes 63）
- 4 服务 :18790/:18793/:18794/:18792 LISTEN；**h3web 实际监听 127.0.0.1:8732**（plist 仍写 8731，已漂移）
- git：HEAD `b24c3b9` == origin/main `b24c3b9`，**已 push 已同步**（旧记"领先 4 commit 未 push"已过期）
- h3web = Python 标库 `BaseHTTPRequestHandler`（3438 行，手写 path 分发，非 FastAPI/Flask），**无 /health 端点** → adapter 需补 `/health` + 能力声明桩

## 下一步（新会话续跑）

三决策已拍板 → 开 P0，顺序 **Agent Registry → 插件运行时 → Adapter(h3web)**。
- 增量 1：规范数据格式 spec（记忆/skill 的 JSON canonical + YAML view + 字段 schema）——互通性地基，**字段草案已定，正式 schema 文件待落 `l2/specs/`**（P0 第一交付物，未落盘）
- 增量 2：Agent Registry CRUD API，以增量 1 的规范为地基（Profile 读写 + 能力标签注册）
- 每模块独立 commit + 真跑验；shadow/编排属 P2 不在 P0 期
- **待 commit**：本次 MEMORY 更新 + `l2/specs/` 尚未入 git（工作区 M 状态）
