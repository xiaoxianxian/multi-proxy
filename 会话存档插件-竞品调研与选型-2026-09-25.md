# 会话存档插件 — 竞品调研与选型（2026-09-25 · 已二次求证）

> 面向 **multi-proxy 项目迭代** 的工作文档。配套：
> - 文章端（写作用）：`~/Documents/AI项目/文章-agent-git深度分析/竞品对比分析.md`（v2，含完整论点/数据/拍砖清单）
> - 集成草案：`~/Documents/AI项目/multi-proxy/AGENTGIT-集成设计草案-2026-09-25.md`（§11 已交叉引用本文件）
>
> 核心命题：**agent-git 不是唯一解，也不是最优选。** 既然 multi-proxy 定位「一切皆插件」，会话存档必须做成一个插件 + 一个 `ConversationArchiveProvider` 接口，外部方案（agent-git / cass / re_gent / 自研）各做 adapter 可插拔，拔插零成本。
> 所有元数据经 `gh api` + LICENSE 原文 + WebSearch **两次复验（2026-09-25 上下午）**。

---

## 1. 本次求证的关键修正（决定选型）

| 项 | 修正结论 | 对 multi-proxy 的含义 |
|---|---|---|
| **cass 协议** | **MIT + OpenAI/Anthropic 排除 rider**（非 OSI 标准，但除这两家外任何人可用/改，含你的项目） | 可用、可 fork；文档写清 rider 边界即可，不再当「高风险」 |
| **Entire 本地性** | **账号/云端控制面绑定**（`entire login` 浏览器登录 + token 派生控制面），非纯离线 | **排除出本地优先默认**；除非用户明确接受账号 |
| **re_gent** | 补录：794★、Apache-2.0、Go、v1.1.0；`rgt blame` 逐行 prompt 归因 | provenance 最强，但**仅支持 Claude Code**，作可选插件 |
| **agenthist** | 补录：120★、MIT、TS、v0.2.2；A+B+C 混合 + 自带 Skill | 跨 agent 转换 + 经验抽取参考 |
| **cass 覆盖** | README 显式 **~26 agent**，含精确存储路径（Hermes `~/.hermes/state.db` 等） | 写 adapter 的**现成路径地图** |
| **Oak** | 仅中文媒体提及，GitHub 仓库搜不到 | 不写入结论，标记待追踪 |

---

## 2. 赛道 5 条路线（决定「组合而非单选」）

- **A. Git 原生版本化**：agent-git、Entire、re_gent、ConvX —— 过程可追溯 + 跨会话接力
- **B. 本地统一检索**：cass、ctx、AgentHistory、chat-recall —— 「我之前怎么解决 X」快速召回
- **C. 提交关联 / 精确溯源**：re_gent（step 级 DAG）、Residue —— 代码改动 ↔ 决策过程 双向追溯
- **D. 导出 / 转储**：agent-dump、shiplog、claude-conversation-extractor、AiSyncing、claude-transcripts —— 最低成本落地
- **E. 知识抽取**：ai-memory、WUPHF、agenthist —— transcript → 可复用知识

> **没有任何单一项目在全部维度胜出。** agent-git 独占「版本化+续跑+分享」且本地优先最干净；re_gent provenance 最强但覆盖为零；cass 检索+覆盖最强但无版本化；Entire 最成熟但账号绑定；agenthist 是混合实用派。

---

## 3. 候选实测元数据（2026-09-25 复验）

| 项目 | ★ | 协议 | 语言 | 版本 | push | 路线 | 多 agent 覆盖 | 本地优先 |
|---|---|---|---|---|---|---|---|---|
| Einsia/agent-git | 357 | MIT | Rust | v0.2.6 | 09-24 | A | 6+（含 WB/Hermes） | ✅ 纯本地 git |
| entireio/cli | 5114 | MIT | Go | v0.11.2 | 09-24 | A | 多 | ⚠️ 账号绑定 |
| regent-vcs/regent | 794 | Apache-2.0 | Go | v1.1.0 | 07-02 | A/C | 仅 Claude Code | ✅ 本地 |
| Dicklesworthstone/coding_agent_session_search | 1146 | MIT+rider* | Rust | v0.8.0 | 09-24 | B | **~26** | ✅ 本地 |
| ctxrs/ctx | 1132 | Apache-2.0 | Rust | v2.0.0 | 09-24 | B/C | 本地 | ✅ 本地 |
| lohoz/agenthist | 120 | MIT | TS | v0.2.2 | 09-19 | A/B/C | 4 | ✅ 本地 |
| ZeroSumQuant/claude-conversation-extractor | 678 | MIT | Python | — | 01-02 | D | Claude Code | ✅ 本地 |
| xingkaixin/agent-dump | 8 | MIT | Python | — | 09-24 | D | 6 | ✅ 本地 |
| butttons/residue | 3 | MIT | TS | — | 02-24 | C | 多（适配器） | ✅ 自托管 |
| nex-crm/wuphf | 1464 | NOASSERTION | Go | — | 09-24 | E | 通用(MCP) | ✅ git wiki |

\* cass 的 rider 仅排除 OpenAI/Anthropic，**对你的项目不构成障碍**。

---

## 4. 选型决策：v1 默认 provider 推荐

| Provider | 角色 | 采用建议 |
|---|---|---|
| **AgentGitProvider** | v1 默认 | ✅ 纯本地 git + WB/Hermes 一等公民 + 功能组合最全。**版本钉死 + 关键读原生格式逻辑 fork 自持**（年轻项目，曾破坏性重写） |
| **CassProvider** | 检索后端（B 路线） | ✅ 挂上做召回增强；~26 agent 覆盖、MIT+rider 可用；其存储路径清单直接喂 adapter |
| **ReGentProvider** | 可选精确归因 | ⚠️ 仅 Claude Code；若未来需「哪行代码哪个 prompt」再接，Apache-2.0 可嵌入 |
| **EntireProvider** | 排除本地优先默认 | ❌ 账号/云端控制面绑定，不符合「数据不出本机」硬诉求；用户显式接受账号时才可选 |
| **SelfBuiltProvider** | 战略路径 | ⭐ 复用 multi-proxy 已有 `session-store.js` + `session-keepalive.js`（M7 已闭环），叠 git-backend adapter，读各家原生目录落盘到自管本地 git。不依赖任何年轻外部项目、协议零风险、完全本地优先 |

> **替换成本归零纪律**：所有外部依赖只过 provider 接口；版本钉死；GUI 一键切换（"拔 agent-git，插 cass"）；满足 L2-BLUEPRINT 四硬约束（通用化/低成本接入/高扩展/兼容性）。

---

## 5. `ConversationArchiveProvider` 接口草稿（推进交付物）

> 放置建议：`multi-proxy-manager/lib/archive/provider.ts`，由 `providerRegistry` 统一注册，GUI 下拉切换。

```ts
// multi-proxy-manager/lib/archive/provider.ts
// 会话存档插件统一接口：agent-git / cass / re_gent / 自研 都实现它

export interface RuntimeInfo {
  id: 'workbuddy' | 'hermes' | 'codex' | 'claude-code' | 'cursor' | string;
  displayName: string;
  nativeResume: boolean;   // 能否原生续跑
  importOnly: boolean;     // 仅归档不能续跑（如 Cursor）
  sessionPath: string;     // 原生存储路径（来自 cass 清单，现成地图）
}

export interface SessionRecord {
  id: string;
  runtime: string;
  project?: string;
  startedAt: number;
  rawPath: string;
  commitSha?: string;      // 若已落盘到 git
}

export interface SearchHit {
  sessionId: string;
  score: number;
  excerpt: string;
  runtime: string;
}

export interface Artifact { kind: 'md' | 'json' | 'html'; data: Buffer | string; }

export interface SecretPolicy {
  dict: string[];          // 仓库级密钥特征（Agnes/DeepSeek key 等）
  placeholder: string;     // 可逆占位符
}

export interface ConversationArchiveProvider {
  /** 探测本机已装 agent 运行时（复用 cass 的 adapter 发现 + 存储路径清单） */
  detectRuntimes(): Promise<RuntimeInfo[]>;

  /** 收编现有原生会话（对抗"已压缩丢失"） */
  import(sourcePath: string): Promise<SessionRecord>;

  /** 逐轮落盘（对抗压缩，hook 或轮询驱动） */
  capture(sessionId: string): Promise<void>;

  /** 跨 agent 召回（路线 B，弱实现可抛 NotImplemented） */
  search(query: string, opts?: { runtime?: string }): Promise<SearchHit[]>;

  /** 续跑 / 跨 agent 接力（asRuntime 空=同运行时续跑） */
  resume(sessionId: string, asRuntime?: string): Promise<void>;

  /** 版本回滚（路线 A） */
  rewind(commitSha: string): Promise<void>;

  /** 提交关联（路线 C，re_gent/Residue 强） */
  linkCommit(sessionId: string, sha: string): Promise<void>;

  /** 转出（路线 D） */
  export(sessionId: string, format: 'md' | 'json' | 'html'): Promise<Artifact>;

  /** 密钥过滤（仓库级字典） */
  redactSecrets(policy: SecretPolicy): Promise<void>;

  /** 本地优先自检：是否纯本地、无外部账号 */
  localFirstCheck(): { local: boolean; requiresAccount?: string };
}
```

### 5.1 各 provider 最小骨架

```ts
// AgentGitProvider —— v1 默认，子进程调用 agit 二进制（bundle + 版本钉死）
class AgentGitProvider implements ConversationArchiveProvider {
  async detectRuntimes() { /* `agit doctor` / adapter registry */ }
  async import(p: string) { /* `agit import` */ }
  async capture(id: string) { /* 轮询/ hook 触发 `agit commit` */ }
  async search() { throw new NotImplemented('agent-git 无检索，挂 CassProvider'); }
  async resume(id: string, as?: string) { /* `agit resume ... --as <as>` */ }
  async rewind(sha: string) { /* `agit rewind` */ }
  async linkCommit() { /* 可选 */ }
  async export(id: string, f: 'md'|'json'|'html') { /* `agit log` + 渲染 */ }
  async redactSecrets(p: SecretPolicy) { /* `agit secrets add` */ }
  localFirstCheck() { return { local: true }; }
}

// CassProvider —— 检索后端，~26 agent 覆盖，MIT+rider
class CassProvider implements ConversationArchiveProvider {
  async search(q: string) { /* `cass search` 语义+词法混合 */ }
  // detectRuntimes / import 复用 cass 对各家的精确存储路径
  localFirstCheck() { return { local: true }; }
  // 其余版本化方法抛 NotImplemented，由 AgentGitProvider 负责
}

// SelfBuiltProvider —— 战略路径，复用 M7 session-store + git-backend adapter
class SelfBuiltProvider implements ConversationArchiveProvider {
  constructor(private store: SessionStore /* M7 已有 */) {}
  async capture(id: string) { /* 读原生目录 → 落盘到自管本地 git 仓库 */ }
  async search(q: string) { /* 接 SQLite+向量 或复用 cass 索引 */ }
  localFirstCheck() { return { local: true }; }
}
```

### 5.2 注册与切换（GUI 一键拔插）

```ts
// multi-proxy-manager/lib/archive/registry.ts
const registry = new Map<string, ConversationArchiveProvider>();
registry.set('agentgit', new AgentGitProvider());
registry.set('cass', new CassProvider());
registry.set('selfbuilt', new SelfBuiltProvider(sessionStore));
// GUI 下拉：选中即激活，拔掉 agentgit 插 cass = 零成本
```

---

## 6. cass 存储路径清单（写 adapter 的现成地图）

cass README 显式给出各 agent 原生存储位置，直接喂 `detectRuntimes()` / `import()`：

| Agent | 原生存储路径 |
|---|---|
| Claude Code | `~/.claude/projects`（Session JSONL）+ macOS `~/Library/Application Support/Claude/claude-code-sessions` |
| Codex | `~/.codex/sessions`（Rollout JSONL） |
| **Hermes** | `~/.hermes/state.db`（SQLite）或项目本地 `.hermes/state.db` |
| **OpenClaw** | `~/.openclaw/agents/*/sessions`（Session JSONL） |
| Cursor | `~/Library/Application Support/Cursor/User/`（SQLite `state.vscdb`） |
| Qwen Code | `~/.qwen/tmp/*/chats/session-*.json` |
| Kimi Code | `~/.kimi-code/sessions/*/*/wire.jsonl` |
| ChatGPT | `~/Library/Application Support/com.openai.chat` |
| Copilot CLI | `~/.copilot/session-state` |

> 这解决了「通用化 / 低成本接入」硬性要求——新 agent 只需补一条路径 + 解析器，即可进入统一存档层。

---

## 7. 与 M7 的协同（不自重造）

- M7 的 `session-store.js` 已管「proxy 托管任务的 checkpoint/续跑」；本插件管「底层 agent 运行时原生会话归档」。
- 协同点：在 M7 checkpoint 边界触发 `capture()`，使「任务续跑态」与「全量会话快照」对齐（见 AGENTGIT 草案 §4）。
- SelfBuiltProvider 直接复用 `session-store.js` 作为存储后端，叠 git-backend adapter 即可，无需另起炉灶。

---

## 8. 下一步（可执行）

1. **P0 实测**（沿用 AGENTGIT 草案 §7）：钉 agent-git 版本 → 实测 `import + commit + resume` 纯离线闭环 → 确认本地优先成立。
2. **出 provider 骨架落地**：把 §5 接口 + §5.2 注册写进 `multi-proxy-manager/lib/archive/`，先实现 `AgentGitProvider`（bundle 二进制 + 版本钉死）。
3. **cass 路径清单入 adapter**：把 §6 清单落成 `detectRuntimes()` 的解析器，作为 `CassProvider` 与 `SelfBuiltProvider` 的共享数据。
4. **Entire 排除**：文档明确「本地优先默认不含 Entire」，GUI 仅在用户接受账号时露出 `EntireProvider`。
5. **文章端取用**：`../文章-agent-git深度分析/竞品对比分析.md` 已就绪，re_gent 的 `rgt blame` 是「会话即审计资产」叙事的强力佐证。

---

*本文件供 multi-proxy 开发迭代 / 写深度分析文 / 营销文时直接取用。数据 2026-09-25 经 `gh api` + LICENSE 原文 + WebSearch 复验。*
