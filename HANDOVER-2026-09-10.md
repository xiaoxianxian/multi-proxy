# HANDOVER-2026-09-10 — cursor 子模块改名 + 路线图文档落地

> 本文件承接 HANDOVER-2026-08-26.md。前情：P0 8/8、P1 7/7、大量 P2 已清零；
> Electron 桌面壳（session 10）+ 代理冲突检测（c94d5e1）已落地。

## 一、已完成并推送（commit `33a542c`，已 push origin/main，本地=远端同步）

### 1. 子模块 `cursor-multi-model-proxy` → `cursor-proxy`（与项目风格统一）
- `git mv` 改目录名，历史完整保留；`package.json` name 同步。
- 全量更新功能性引用：`Dockerfile.cursor`、`Dockerfile.manager`、`docker-compose.yml`、
  `install.sh`、`manage.sh`、`lib/process-manager.js`（建 cursor 进程配置的目录名）、
  `tests/unit/e2e-scenarios.test.js`、`agent-owner.test.js`，以及
  `CLAUDE / README / USER-GUIDE / FUNCTIONS / ITERATION-ROADMAP / MEMORY /
  architecture.html / ACCEPTANCE-CHECKLIST / PROJECT-STATUS`。
- **两处刻意不动**：
   - `cursor-proxy/src/utils/crypto.ts` 的加密盐 `'cursor-multi-model-proxy-salt'`——
     它是 AES-GCM 的 PBKDF2 盐，改了会让 `data/proxy.db` 里已加密的 API Key 解不开。
   - 历史快照 `review-2026-08-24/`、`HANDOVER-2026-08-24/25/26.md` 保留旧名（项目约定）。
- 完成 proxy-rebuild → multi-proxy 改名收尾：删 `.trae/skills/deploy/SKILL.md`、
  容器名 `multi-proxy-*`、desktop 默认路径。

### 2. 安全：`data/proxy.db` 解除版本跟踪
- 该库含加密 API Key。`cursor-proxy/.gitignore` 早有 `data/*.db`，但 **git 对已跟踪文件
   不应用 ignore**，所以它一直挡不住。`git rm --cached -f`（本地保留 57KB，不影响运行）；
  `.gitignore` 另加根 `providers.json`。全库已无密钥/数据类文件入库。

### 3. 新增规划资产（为方向四「任务类型智能派发」预埋）
- `ITERATION-ROADMAP.md`：4 个迭代方向（①DAG 健康依赖图 ②动态重规划 ③错误模式检索
  ④内容感知派发），建议顺序 M1→M2→M4→M3→M5→M6。
- `multi-proxy-manager/providers.sample.json` + `PROVIDERS-README.md`：Agnes(免费) →
  GLM(付费) → qwen3.8-local(本地) 三层路由模板，含 `pricing`/`rateLimit` 前向兼容字段
  （当前代码尚未消费，仅预留）。

## 二、测试基线（本轮实测全绿）
| 模块 | 命令 | 结果 |
|------|------|------|
| codex-proxy | `cd codex-proxy && npx jest --forceExit` | 36/36 ✅ |
| hermes-proxy | `python3 -m pytest tests/ -q` | 63/63 ✅ |
| cursor-proxy | `cd cursor-proxy && PATH="$HOME/.local/bin:$PATH" NO_PROXY='*' NODE_OPTIONS=--experimental-vm-modules npx jest --forceExit` | 76/76 ✅ |
| multi-proxy-manager | `cd multi-proxy-manager && npx jest --forceExit` | 396/396 ✅ |
| **合计** | | **571/571 全绿** |

Live 冒烟：`manage.sh start` 四服务全起；`cursor-proxy` 从新目录 18794 启动，
`/health` 200、`/v1/models` 200。冒烟后已 `manage.sh stop` 恢复静止态。

## 三、环境状态（09-10 会话结束）
- 4 端口当前全停（静止态，需时 `manage.sh start`）。
- Node v22.22.3（`~/.local/bin/node`），匹配 better-sqlite3 ABI 127，**无需 rebuild**。
- git：本地 = `origin/main` = `33a542c`，无 ahead/behind。

## 四、下一步（规划中，未开工）
1. **M1 方向① DAG 健康依赖图**：每个 proxy 暴露 `/api/health` 分级状态（ok/degraded/down +
   各检查项），manager 轮询改调 health endpoint，dashboard 徽章升级为「带原因标签」。
   无依赖，基础设施，建议先做。
2. **M6 方向④ 任务类型智能派发**（最直接对应老板「不同任务自动选模型」诉求，可与 M1 并行）：
   实现 `routeEngine.ts` 的 `classifyTask` + `evaluateRules`（白名单解析器，禁 eval）+
   `cost-optimization` 分支 + 修 `currentRouteIndex` 跨 proxy 串号 bug；`getNextRoute`
   签名扩展为 `(taskType, modelName, config)`。
   资产已备：`providers.sample.json`。

## 五、📖 新会话开场白（直接复制）
```
读取 /Users/xiaota/Documents/AI项目/multi-proxy/HANDOVER-2026-09-10.md 了解进展。
然后从 ITERATION-ROADMAP.md 的四方向里挑一个实现（建议 M1 或 M6），每步完成后跑
全量测试（571/571 基线）+ live 冒烟验证。
测试矩阵见 skill:proxy-rebuild-dev（cursor 已改名 cursor-proxy，注意路径）。
```
