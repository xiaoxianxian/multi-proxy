# Proxy Rebuild 第四轮会话交接 — P1 评审清单清零（2026-08-26）

> 本文件是第四轮会话交接。前情见 HANDOVER-2026-08-25.md（第三轮，P0 8/8 清零）。

## 一、本轮完成（12 个文件，155 行 +，1 个 commit）

| # | P1 项 | 内容 | 文件 |
|---|-------|------|------|
| 1 | P1-1 | 忘记密码指引路径修正（`~/.multi-proxy-password` → `~/.multi-proxy-manager/password`）+ 文档/测试同步 | login.html, README.md, USER-GUIDE.md, ACCEPTANCE-CHECKLIST.md, login.test.js |
| 2 | P1-2 | providers.json 权限收紧 0600（原子写前 chmod） | codex-proxy/proxy.js, hermes-proxy/proxy.py |
| 3 | P1-3 | server.js 三处 delete 后判空死代码修正（先 kill 再 delete，僵尸进程泄漏修复） | server.js:711,784,853 |
| 4 | P1-4 | server.js 新增错误处理中间件（JSON 解析错误返回 400 JSON，不泄 Node 堆栈） | server.js:1425 前 |
| 5 | P1-5a | login.html 暗色模式初始化（localStorage 读取 darkMode 偏好） | login.html |
| 6 | P1-5b | proxy-config.html 字体加载链补齐（preconnect + Google Fonts link） | proxy-config.html |
| 7 | P1-5c | proxy-config.html 暗色模式初始化（init() 中调用 applySavedDarkMode()） | proxy-config.html |
| 8 | P1-6a | proxy-config.html history 断头路：MOCK_HISTORY → loadHistory() 真实 API | proxy-config.html |
| 9 | P1-6b | proxy-config.html clearHistory → POST /api/clear-history | proxy-config.html |
| 10 | P1-6c | proxy-config.html switchModel → codex/hermes 走 /switch-model（改配置+记历史+路由模式切 config）；cursor 保留 settings PUT | proxy-config.html |
| 11 | P1-6d | proxy-config.html switchCurrentModel → await 返回值，成功提示，失败回滚 UI | proxy-config.html |
| 12 | P1-6e | proxy-config.html loadConfigData → codex/hermes 用 /api/config（model 字段），cursor 保持 /admin-api/settings | proxy-config.html |
| 13 | P1-6f | logs.html 工具栏新增 TXT/CSV 导出按钮（window.exportLogs 原已实现但未挂载） | logs.html |
| 14 | P1-7 | cursor SQLite 新增 idx_logs_timestamp + idx_models_name 热路径索引（database.ts D3 迁移块） | cursor-multi-model-proxy/src/db/database.ts |
| 15 | P1-8 | test-connection 端点参数名统一（providerId + 兼容旧 model 字段） | server.js:1130 |

## 二、测试基线（本轮实测全绿）

| 模块 | 命令 | 结果 |
|------|------|------|
| codex-proxy | `cd codex-proxy && npx jest --forceExit` | 36/36 ✅ |
| hermes-proxy | `python3 -m pytest tests/ -q` | 63/63 ✅ |
| cursor-multi-model-proxy | `PATH="$HOME/.local/bin:$PATH" NODE_OPTIONS=--experimental-vm-modules npx jest --forceExit` | 76/76 ✅ |
| multi-proxy-manager | `cd multi-proxy-manager && npx jest --forceExit` | 389/389 ✅ |
| **合计** | | **564/564 全绿** |

## 三、Live 冒烟验证（2026-08-26）

重启 manager 后实测：

```bash
# P1-4: 坏 JSON 不再泄堆栈
$ curl -s -X POST http://localhost:18792/api/auth/login -H 'Content-Type: application/json' -d '{invalid json'
{"success":false,"error":"Invalid request body: malformed JSON"}
HTTP 400  # 以前是 500 + 完整 Node 堆栈

# P1-8: 新参数名 providerId 生效
$ curl -s -X POST http://localhost:18792/api/test-connection \
    -H "x-auth-token: $TOKEN" -H 'Content-Type: application/json' \
    -d '{"providerId":"openai","baseUrl":"http://127.0.0.1:9","apiKey":"sk-test"}'
{"success":false,"error":"无法连接到供应商 API，请检查网络和地址（ECONNREFUSED）"}
# 文案正确（网络错误分类），不是参数错误

# P1-6: history 端点可用
$ curl -s http://localhost:18792/api/codex/api/history -H "x-auth-token: $TOKEN"
{"history":[]}
$ curl -s http://localhost:18792/api/hermes/api/history -H "x-auth-token: $TOKEN"
{"history":[]}
$ curl -s http://localhost:18792/api/cursor/admin-api/settings -H "x-auth-token: $TOKEN"
{"settings":{"routing_mode":"round-robin","rr_index":"0"}}
```

## 四、P1 清单全部清零

`review-2026-08-24/00-SUMMARY.md` 的 7 项 P1 现已全部修复并提交。

剩余未动项（按严重程度排序）：

### 🟡 中优先级（建议下轮考虑）
1. ~~server.js 1414 行单文件巨石~~ ✅ 已修（588d10d，拆分为 lib/ 4 模块 + routes/ 4 路由文件，server.js 瘦身为 105 行组装层）
2. **codex/hermes 70% 同构代码漂移** — generateId / api_key 掩码 / 错误码大小写不一致（P1-1 只统一了格式）
3. ~~B6 Token 过期无全局 401 处理~~ ✅ 已修（baec75b，三页面 fetch 401 拦截跳登录）
4. ~~B8 API Key 明文进 DOM~~ ✅ 已修（baec75b，key 不进 DOM，点击从内存按 id 取）
5. ~~硬编码颜色逃逸遍布四页~~ ✅ 已修（7e2c85e，colors_and_type.css 新增代理品牌令牌 + deep 对比度令牌，dashboard/logs/proxy-config/shared-styles 替换 13 处硬编码）
6. ~~login.html header 有 `data-theme-toggle` 但缺 dark mode toggle 按钮~~ ✅ 已修（8dbd0fa，右上角固定圆钮，与 dashboard 共用 darkMode 键）
7. ~~`.btn:focus-visible` 样式缺失、proxy-config 动态 modal 无 `role="dialog"` / `aria-modal` / Esc 关闭~~ ✅ 已修（8dbd0fa）

> **评审误报澄清（重要）**：8-UI设计师.md 指控的「`apiKey: ***` 是非法 JS
> 语法，导致 proxy-config 整段脚本无法解析」为**评审工具脱敏假象**——真实文件
> 内容是 `apiKey: key` / `apiKey: apiKey`，语法合法，页面 JS 从未损坏。已用
> `new Function(script)` 全脚本校验 + 逐字符核验确认。后人勿再被误导。

### 🟢 P2 排期优化
- 无数据库迁移框架（PRAGMA user_version）、无备份策略、日志无轮转
- 间距/字号/z-index 无设计令牌（**B5 已部分清偿：硬编码颜色逃逸**）
- 可访问性欠账（焦点态/键盘导航）
- 版本号硬编码、fault 状态不上 UI、confirm() 与自定义 modal 混用

## 五、环境状态（2026-08-26 会话结束时）

- 本机直跑 4 服务运行中：codex:18790 / manager:18792 / hermes:18793 / cursor:18794
- Manager 密码：test-pass-12345（存 `~/.multi-proxy-manager/password`）
- `~/.multi-proxy-manager/providers.json` 权限：`-rw-------`（0600）
- Docker：proxy-rebuild-* 4 容器已停止（未删除）
- cc-switch 15721 未受影响

## 六、已知遗留风险（交接备注）

1. **P1-6 history 端点仅 codex/hermes 支持**，cursor 无对应端点，UI 会显示"当前代理不支持切换历史"空态文案
2. **P1-6 switchModel 成功后历史需 reload**：`switchCurrentModel` 成功后调 `loadHistory()` 刷新列表
3. **P1-5 暗色模式**：login.html 与 dashboard/logs/proxy-config 共用 `localStorage.darkMode` 键，但 login 页无 toggle 按钮（用户只能在 dashboard 切换后，登出再登录时生效）
4. **P1-8 向后兼容**：test-connection 同时接受 `providerId` 和 `model` 两个参数名（优先 providerId）

---

# 📖 新会话开场白（直接复制）

```
读取 /Users/xiaota/proxy-rebuild/HANDOVER-2026-08-26.md 了解进展。
然后处理第六节的遗留风险或下一轮评审任务。
每步完成后跑全量测试验证。
```
