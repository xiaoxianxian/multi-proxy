# MEMORY 09-26 — 全量评估 + WorkBuddy 战略批入库

> 供 Hermes/WorkBuddy 跨会话续跑。本日 = 全项目评估 + 5 份 WorkBuddy 战略文档入库 + 2 处数字缺口。
> 上游：MEMORY-2026-09-25.md（Q3 成本闭环 / timer-flaky / DSH sync / 3 设计稿 / DeepSeek 核实）。

## 一、本日已完成（全部已 commit）

| commit | 内容 | 状态 |
|--------|------|------|
| `57a4fe5` | AGENTS.md §6 测试总计 → 1051 体系（python3 直写，patch 静默回退的绕过法） | 已 push |
| `b158acd` | WorkBuddy 战略评估批 5 份：03-token-control-plane / V1-SCOPE-BOUNDARY / LICENSE-STRATEGY / PENDING-DECISIONS / INDEX 指针 | 已 push |
| `22bfa99` | P1 数字同步 1051 体系 | 已 push |
| `6c49b5c` | P2 DSH 术语统一（DeepSeek Harness + 脚注） | 已 push |
| `38f190c` | P3 ROADMAP 笔误订正 | 已 push |
| `9782c75` | INDEX §7 变更日志 + last_updated→09-25 | 已 push |

**本地 = origin/main = `b158acd`，工作区全干净（0 untracked / 0 modified）。**

## 二、本轮真跑数字真值（AGENTS §4 实跑，非摘录）

- cursor-proxy：`219 tests / 17 suites`（NODE_OPTIONS=--experimental-vm-modules，09-26 真跑全绿）
- l2 demo：`318 checks / 23 demo`（find 全扫 awk 求和，含 savings-gateway 子目录 24 + cost-watchdog 34）
- jest 合计 1051 = manager 770/47 + codex 62/6 + cursor 219/17

## 三、WorkBuddy 战略批（b158acd）— 5 份文档定位

1. **03-token-control-plane.md** — 代理原生 token 控制平面设计稿。结论：核心通用层 + 可选场景包(curated 非开放平台)；TokenPolicy hook 草案；竞品研判（LiteLLM 压住通用层、Context Gateway 先行）；ROI 路线图 P0-P3。**设计稿不写码。**
2. **V1-SCOPE-BOUNDARY.md** — v1 = 架构完整/链路通/模块齐，量化 5 条可验收判据 + 7 类 v2 排除项。
3. **LICENSE-STRATEGY.md** — 开源协议：主推 AGPL-3.0（防大厂白嫖）+ 可选商业双许可。决策文档，**暂不生成 LICENSE**。
4. **PENDING-DECISIONS.md** — 两项待拍板入口：待办1 协议选型 / 待办2 v1 收口就绪度评估。
5. **INDEX.md** — 新增「当前待决策」指针段。

## 四、⚠️ 本轮发现的 2 处数字缺口（待老板拍板后修）

**缺口 A — `01-feature-matrix.md` 顶层未同步（P1 漏改上半部）**
- P1 `22bfa99` 只改了 L70 附近「09-21 增量」段，**顶层仍 stale**：
  - L4 `最后验证：2026-09-16, HEAD b69fc77` → 应 09-25/今
  - L13 `L2 核心模块 | 10 | 13 demo 全 PASS` → 应是 23 demo / 318 checks
  - per-module 小计（alert 13/13、cost 14/14 等）是各 module 自身 check 数，暂疑仍准
- 修法：顶层 L4 日期 + L13 demo 数同步到 09-25 真值。

**缺口 B — WorkBuddy 新文档 cursor 数字错（193 应为 219）**
- `V1-SCOPE-BOUNDARY.md:40` 与 `PENDING-DECISIONS.md:40` 都写 `cursor 193`
- **193 是旧的 l2 demo 总数（~193），被误当 cursor 测试数**；cursor 真值是 219/17
- 修法：两处 `cursor 193` → `cursor 219`。

## 五、02-L2热路径改造计划（核心「待启动」项，已全读）

6 个 Step（仅方案未落码），门控默认关/shadow 默认开，不破铁律：
- Step 1 registry 单例注入（4 个 AgentRegistry 分裂 → 1 份）
- Step 2 HTTP 面走 async 分派（decompose/orchestrate 不再 -32602）
- Step 3 修误导性错误数据（available 工具列表真实）
- Step 4 route() 纯决策化（防双执行）
- Step 5 plugin 面 bootstrap（让「一切皆插件」真活）
- Step 6（可选）MCP 资源面
> Step 1-3 = 「让 L2 从旁路变可调」核心；Step 4-6 加固。**这是当前最该推进的工程项。**

## 六、下一步（待老板拍板）

拍板清单见本轮报告（PENDING-DECISIONS 待办1/待办2 + 02 热路径 + 2 处数字缺口 + 03 token 控制平面起步）。
老板拍板后逐个推进，每项独立 commit + 真跑验证。
