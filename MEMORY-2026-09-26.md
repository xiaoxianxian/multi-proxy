# MEMORY 09-26 — 全量评估 + WorkBuddy 战略批入库 + ①双许可 + ③ L2 热路径 + ④ token 控制平面

> 供 Hermes/WorkBuddy 跨会话续跑。本日 = 全项目评估 + 5 份 WorkBuddy 战略文档入库 + 协议双许可落地 + 测试数字全项目同步 + ③ L2 热路径改造 Step 1-3 + ④ token 控制平面 P0 PoC。
> 上游：MEMORY-2026-09-25.md（Q3 成本闭环 / timer-flaky / DSH sync / 3 设计稿 / DeepSeek 核实）。
> 续跑锚点：本地 main ahead origin/main 2 commits（`c0e342c` ③ + `b3edf24` ④），未 push（见「七、git 状态」）。

## 一、本日已完成

| commit | 内容 | 状态 |
|--------|------|------|
| `57a4fe5` | AGENTS.md §6 测试总计 → 1051 体系（python3 直写,patch 静默回退的绕过法） | 已 push |
| `b158acd` | WorkBuddy 战略评估批 5 份：03-token-control-plane / V1-SCOPE-BOUNDARY / LICENSE-STRATEGY / PENDING-DECISIONS / INDEX 指针 | 已 push |
| `22bfa99` | P1 数字同步 1051 体系 | 已 push |
| `6c49b5c` | P2 DSH 术语统一（DeepSeek Harness + 脚注） | 已 push |
| `38f190c` | P3 ROADMAP 笔误订正 | 已 push |
| `9782c75` | INDEX §7 变更日志 + last_updated→09-25 | 已 push |
| `f27ac9d` | MEMORY-2026-09-26 日志首轮 | 已 push |
| `3571825` | 数字缺口修正(缺口A feature-matrix 顶层 + 缺口B cursor 193→219) | 已 push |
| `c3a2ee1` | ① 协议双许可(LICENSE + README §License)+ 数字全同步(README §10/quickstart/consistency)+ 日志 | 已 push |
| `c0e342c` | ③ L2 热路径改造 Step 1-3(非侵入,门控默认关/shadow)：registry 单例 + HTTP async 分派 + 修 -32602 | 本地,未 push |
| `b3edf24` | ④ token 控制平面 P0 PoC：prompt-cache 内核 + demo/jest + 03 §8 落盘(门控默认关/零热路径) | 本地,未 push |

## 二、本轮真跑数字真值（AGENTS §4 实跑,非摘录）

- cursor-proxy：`219 tests / 17 suites`（NODE_OPTIONS=--experimental-vm-modules）
- l2 demo：`318 checks / 23 demo`（③前）→ `328 checks / 24 demo`（④后，+10 prompt-cache）（find 全扫求和，含 savings-gateway 24 + cost-watchdog 34）
- jest 合计 1051 → 1061 体系 = manager 770/47 → 780/48（④后）+ codex 62/6 + cursor 219/17；+ pytest 77/4 + bats 11(历史) ≈ 1390
- ③ 改前后均 770/47 + 318/23（0 回归）；④ 改后 780/48 + 328/24（0 回归，新增 prompt-cache 10 tests + 10 checks）

## 三、① 协议双许可（已落地 + 已 push c3a2ee1）

- **落地方式**：用 GitHub 标准做法——`LICENSE` 文件 = 纯 AGPL-3.0 官方全文（34523 字节/661 行,sha 匹配 gnu.org,完整合法），GitHub 仓库页自动识别 License 类型；双许可（商业授权）声明放 README `## License` 章节（按 LICENSE-STRATEGY §5.2 模板）。
- **为何不写 LICENSE-AGPL.md**：双许可的"开源版 AGPL + 商业授权"靠 README 章节讲清商业授权入口(联系 281330913@163.com)即可,不必单独文件；LICENSE 文件放纯 AGPL-3.0 是为了让 GitHub 标出 License 类型(单放 AGPL+商业声明会干扰识别)。
- **版权署名**：git 作者 `xiaoxian <281330913@163.com>`(README 章节已引用)。
- **未做(可选增强,记 backlog)**：逐源文件加版权声明——非 AGPL 强制,本批不做(避免上百文件侵入扩散)。
- **依据文档**：`docs/04-business/LICENSE-STRATEGY.md`(主推 AGPL-3.0 防大厂白嫖 + 保留商业双许可收费权)。PENDING-DECISIONS 待办1 已答结论 = **选 B 双许可**。

## 四、本轮数字缺口全修（全项目终扫 0 stale）

- **缺口 A**（01-feature-matrix 顶层）：L4 日期→09-26、L13 demo 数→23/318。
- **缺口 B**（cursor 193→219）：V1-SCOPE-BOUNDARY:40 + PENDING-DECISIONS:40 两处(3571825 已修)。
- **本轮新增修**：
   - README §10 测试表 + 项目状态表 → 1380 体系(770/62/219/77/l2 318),残留 635/882/722/55/131/182 全清零。
   - `docs/quickstart.md` 命令块 4 处旧数字(722/55/193/63)→ 770/62/219/77(活命令预期输出须同步)。
   - `docs/09-review/consistency-report.md`：**文首加 superseded 注**(它是 09-17/09-18 历史快照,内部 635/131 数字如实保留,不改历史只标注)。
- **全项目终扫**：grep 11 个 stale 模式(archive 排除)= **0 命中**。

## 五、② v1 冻结就绪度评估（对照 V1-SCOPE-BOUNDARY.md 5 条判据）

| 判据 | 核查 | 结论 |
|------|------|------|
| 1 四层覆盖 | 代理/网关/中台/管理台各≥1 可运行模块 | ✅ 已满足 |
| 2 端到端链路 | 代理→网关→中台→执行→回结果 | ✅ 已满足 |
| 3 管理面板 | 18792 看 3 proxy 状态+启停+日志+配置 | ✅ 已满足 |
| 4 测试基线全绿 | 770/62/219/77 + l2 328(09-26 ④后实跑) | ✅ 已满足 |
| 5 对外定位文档就绪 | 独立"中台+网关+治理"定位文档 | ⏳ **缺口**:README 有定位但非独立推广用定位文档 |

**结论**：v1 **架构/链路/测试三件套已就绪**,发布前仅差**补 1 份对外定位文档**(轻量,~30min)。
**建议路径**：补定位文档 → 你最终拍板发布 → 进冻结。冻结/发布是里程碑动作,等你明确指令(不擅自触发)。

---

## 六、③ 02 L2 热路径改造 + ④ token 控制平面（✅ 本会话已落地 + 已 commit · 未 push）

### 权威裁定（accuracy 铁律触发）
09-26 日志原 §六（压缩前二次转述）把 ③ 描述成 `route-engine-orchestrator`/`PROXY_ROUTE_ENGINE_ORCH`/`l2/server.js 收编 4 registry`/`plugin-runtime 加 orchestrate`——这套在 02 原文（59 行实读）里**全不存在**，且 `l2/server.js` 在 c3a2ee1 中不存在（02 第 12 行列的 4 个 AgentRegistry 是 `routes/mcp.js:31`/`routes/registry.js:19-20`/`l2/mcp-server.js:71,494`/`l2/savings-gateway/gateway.js:87`，对照源码全命中）。按 accuracy 铁律：**source（02 原文）> 二次转述（日志 §六）**，本会话以 02 原文 Step 1-3 实施。原 §六 转述已作废。

### ③ 02 L2 热路径改造 Step 1-3（02 原文权威）→ commit `c0e342c`
- 现状（02 §一）：4 个 `AgentRegistry` 分裂；`decompose`/`orchestrate` 经 HTTP 恒 `-32602`；"一切皆插件"生产空集。
- **Step 1 · registry 单例注入**：`routes/registry.js` 导出共享单例 + 幂等 seed（`registry.count()===0` 才 seed，函数本幂等 `mcp-default-seed.js:46`）；`routes/mcp.js` 的 `server()` 注入该单例，不再各 `new AgentRegistry()`。⚠ **诚实边界**：`gateway.js:87`（在 `l2/` 18795 独立进程）未跨层 `require` manager 的 registry——保留它的 `opts.registry` 注入缝但不建 L2→manager 反向依赖（违反非侵入），**待老板定夺是否要真共享**。
- **Step 2 · HTTP 统一 async 分派**：`routes/mcp.js` 的 `/call`→`await s.callToolAsync(...)`、`/rpc`→`await s.handleMessageAsync(...)`；同步入口保留（callToolAsync 对非 async 工具委托回同步 callTool，行为超集不回归）。
- **Step 3 · 修误导错误数据**：`l2/mcp-server.js` 两处 `-32602` 的 `available` 改 `s.listTools().map(t=>t.name)`（含 routeTask/decompose/orchestrate，不再缩成 5 个 adapter）。
- 验证：manager jest `770/47` → 仍 `770/47` 全绿；l2 demo `318/23` → 仍 `318/23`（均 0 回归）。门控默认关 / shadow 默认开；热路径 `forward.js`/`codex-proxy/proxy.js` **未碰**；`git add --` 显式 3 文件。
- **根因记录**：首轮 patch 后 `mcp-route.test.js` 15 个全 500 → 复现脚本定位 `TypeError: getSharedRegistry is not a function at server (mcp.js:32)`（registry.js 只挂 `module.exports = router`，漏挂新 helper）→ 补 `module.exports.getSharedRegistry = getSharedRegistry; module.exports._resetSharedRegistry = ...` 修复。教训：**patch 改 module.exports 时必须把新增函数显式挂到导出对象**（不能只 `module.exports = router`）。

### ④ token 控制平面 P0 PoC → commit `b3edf24`
- 03（226 行设计稿）§5 P0 = prompt-cache 断点 PoC；§7 列 5 个待定/待拍板项（hook 签名 / 预算上限 / agent 类型枚举 / 是否真注入 upstream / 是否先落 P0）。本会话已落 P0 第一项。
- **已落**：`l2/prompt-cache.js`（前缀哈希纯 JS CRC32 缓存命中检测 + 断点注册表 + cacheHitRate 报告，零依赖）+ demo 10 checks + jest 10 tests。门控 `PROXY_PROMPT_CACHE` 默认 off = observe（内存计数、绝不落盘）。前缀 key = `systemPrompt + toolSchemas + messages[0]`，`messages[1:]` 不进 key。
- 实测：manager jest `770/47` → `780/48`（+10 prompt-cache，0 回归）；l2 demo `318/23` → `328/24`（+10，0 回归）。门控验证（demo 1/9/10）：默认 off→`isGateOpen()===false`，`=1`→`true`，关时 `report().gate===false`。零热路径、不接现有 HTTP 路由。
- **诚实边界（非 03 §1.1 设计目标）**：03 §1.1 "cacheHit 28.7%" 是设计目标，全仓 `cacheHit`/`cache_control` 0 命中实现，本 PoC 从 0 建基线；`estTokensSaved` 按 0.9 模拟非真上游返回值；不真注入 upstream body、不接现有路由；§7 待定项（尤其"是否推广到路由挂载/真上游注入"）待老板拍板。03 已加 §8 落盘记录。

### 铁律合规（全部满足）
- 热路径 `forward.js`/`codex-proxy/proxy.js` 未碰。
- `git add --` 显式列文件，绝不 `-A`；两 commit 分别 3/4 文件，每批先 `git status --short` 确认恰好预期数。
- 每个数字真跑（jest/l2 demo 改前后各跑），不抄；§1.1 28.7% 标注为设计目标非既成。
- push 前需老板确认：**当前本地 ahead origin/main 2 commits（`c0e342c` ③ + `b3edf24` ④），未 push**。

---

## 七、git 状态（③④ 后）

- `c3a2ee1`：① 协议双许可 + 数字全同步（已 push，本地=origin/main 同步点）。
- `c0e342c`：③ L2 热路径 Step 1-3（3 文件：`routes/registry.js` / `routes/mcp.js` / `l2/mcp-server.js`；`git add --` 显式，绝不 -A）。
- `b3edf24`：④ token 控制平面 P0（4 文件：`l2/prompt-cache.js` / `l2/prompt-cache.demo.js` / `multi-proxy-manager/tests/unit/prompt-cache.test.js` / `docs/10-initiatives/03-token-control-plane.md` §8）。
- 工作区 0 残留。**push 待老板确认**（铁律：不擅自推远端）。

### 待老板定夺（③④ 遗留）
1. ③ Step 1 联动 `gateway.js:87`：是否要 L2 子进程反向共享 manager registry（当前保留注入缝、不建反向依赖）。
2. ④ §7 待定项：prompt-cache PoC 是否推广到路由挂载 / 真上游 `cache_control` 注入。

### ③④ 后续续跑提示词（如需新窗口继续 P1+）
> 我在推进 multi-proxy ③ L2 热路径（已落 Step 1-3，commit c0e342c）+ ④ token 控制平面 P0 PoC（已落 prompt-cache，commit b3edf24，详见 MEMORY-2026-09-26 §六 + 03 §8）。请读 `docs/10-initiatives/02-L2热路径改造计划.md` 的 **Step 4-6**（route() 纯决策化 / plugin bootstrap / MCP 资源面）与 03 §5 P1+。
> **铁律**：热路径 `forward.js`/`proxy.js` 不碰；`git add --` 显式列文件绝不 `-A`；每个数字真跑不抄；push 前需老板确认；门控默认关/shadow 默认开。
