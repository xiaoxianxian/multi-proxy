# 2026-09-25 daily log · Q2 module-state 根 fix 收口

## 背景
老板 09-25 采纳推荐「先等 CI 绿 → Q2 module-state 根 fix → Q3 cost 实采殿后」,说「好,接受你的建议,继续执行」,本轮开 Q2。

## 找到并修了真 bug(KB dead-code)
- `routes/knowledge-base.js` 的 `_resetForTest` 原本设在 `module.exports = router` **之前**,被 `module.exports = router` 覆盖成 **dead code** → 它自己 test(`tests/unit/knowledge-base-route.test.js:18` 的 `router._resetForTest()`)的 **self-cleanup 长期失效**,是 09-24 2/30 串味的一个真实根。
- 移到 `module.exports = router` 之后 → **commit `cbbe344`**。

## 残余 flaky 量化(09-25 实测)
- 串行 `--runInBand` 10 轮:**10/10 全绿**(KB fix 治好跨文件 module-state 串味)。
- 并发 `--maxWorkers=4` 15 轮窗:**13/15(2 真失败)**;10 轮窗 **0/10** → 残余 <13%,单 test 偶发,属 §13.21「**真 timer-flaky**」(需 `jest.useFakeTimers()` 或接受 ~3% 容差,单独排期)。
- **实验 `afterEach` 万能 reset(5 route + 5 lib)** → 引发 `sessions-api` **100% 回归**(清 sessionStore 破坏 in-suite 闭环;sessions/orchestration/skill-service 三 route 本就自带 self-cleanup,打架)→ **已回退弃用**。Q2 是 system 级架构矛盾,非「加几个 reset」能解。

## 收尾
- INDEX §7 收口行 commit `1de02b5`。
- 四模块回归全真跑:codex 62/62 · cursor 193/193 · hermes 77/77(须用有 flask 的 python3,本地 `python` 指向无 flask 的 uv=误报)· l2 agent-registry 27 + mcp-server 11 + route-engine demo `✅`。
- 两个中途「异常」均查清是误报(hermes flask 解释器 / l2 demo 文件名猜错),非回归。
- push 链:`cbbe344` + `1de02b5` 全 push,`main` ahead 0 = origin/main。
- CI 红绿按老板指示挂「整体验收出验收步骤」里程碑,不现查。
- Q3 cost 实采:最殿后未动(§13.13 cost 接线,cursor-proxy 侧新接 cost 信号源,需先定接口形态门控默认关 observe)。

## 下一步(Q3)
- Q3 = §13.13 cost 实采接线(P1-B 成本闭环)。forward.js:166-168 已 accumulate 就位。
- cursor-proxy 侧:`lib/pricing.js` / `db.ts getDailyCost` 是现成桩,需接 cost 信号源(mock→provider-health 实采),门控默认关 observe,不碰热路径。
- 需先定接口形态,单独一轮。

## 给后续 agent 判据
- Q2「真 bug」= KB `_resetForTest` dead-code(已修 `cbbe344`)。
- 残余 flaky = timer-based(非 module-state,§13.21 已定性),不值得追。
- `afterEach` 万能 reset 是反模式(破坏 in-suite 闭环),勿再试。
- 大文件(MEMORY.md 136KB)纯 append,禁用 patch 工具;patch 工具只对 proxy.js/routeEngine.ts/agent-registry.js 等非大文件。
- git 铁律:`git add --` 显式列文件,绝不 `-A`;JEV untracked 不碰;push 需老板授权。
- 数字必须真跑不抄(AGENTS.md §4);hermes 77 须 `python` 指向有 flask 的解释器。

## 【续】09-25 下午会话 · Q3 cost 闭环 + timer-flaky 根治 + CI retry + 5 点清单

### 一、Q3 成本闭环收口（已 push `34913d4`→`2667c8e`→`e98fbae`）
- manager 4 文件（`34913d4`）：`proxy-control.js` 补 `fetchCursorActualCost(running)`（仿 /status running 守卫 + `pm.fetchProxyApi('cursor','/admin-api/cost')` best-effort null，读 `totalCny` 真实字段非 `totalCost`）；`alert.js` 第 4 路 `collectCursorCostSignals` `totalCost→totalCny`；test mock 改 `totalCny:20`；前端 `proxy-config.html` 去 `MOCK_BALANCES` USD 死态 + `>0` 守卫（NaN 比较恒 false 不渲染假卡）。
- cursor 7 文件（`2667c8e`）：costTrack/actualCost/chatHandler/admin-api/cost + 3 测试，全绿。
- docs 5 文件（`e98fbae`）：Q3 战略定位 + L2 热路径计划 + 2 战略备忘。

### 二、timer-flaky 根治（已 push `67b1e06`）
- **真凶定位修正**：alert-router `startCostScheduler` 抽 `tick()` 闭包 → 测试 `await h.tick()`×3 替代 `setTimeout(40ms)` 等真实 timer（alert-router 30/30 干净）。
- **跨 test module-level flaky ~10%**（串行 `--runInBand` 也 3/30，受害者 knowledge-base/logs/alert 轮流中弹）→ **老板拍板接受 + CI retry**（`--rerunFailsAtMost=2`，12/12 压平，不深修）。§13.31/§13.33 定性已纠：非单点 timer，是模块级共享状态；`afterEach` 万能 reset 仍证伪（引 100% 回归）。
- CI 新增 `manager` job：`npm ci` + `jest --rerunFailsAtMost=2`。
- manager 真实 **770/47**（test-cases.md 仍 635→滞后；DeepSeek 说"701"也不准，真值 770）。

### 三、dsh-web-ui sync + 旧残留删除
- 落后 2689 commit → `git stash -u`（11 lib/client.js 构建产物 `dsh-814-build-artifacts`）→ `pull --ff-only` → v0.4.2 `41974c8c`。
- 4 项 handoff bug 全不存在（架构卫星化/整删：generated/skins.ts 删、eval=0、live-stats 退役）。
- 5 旧残留目录删除（备份 `/tmp/dsh-backup-2026-09-25/`）：dsh-aionui-panel/dsh-live-stats/dsh-pet/dsh-web-ui-compat/dsh-web-ui-settings + satellites/dsh-pet。spec 内是 fixture 字符串非 import，安全。
- 新 blocker：`@deepseek-ai/dsh-client-ui-settings-account@0.1.7-rc.2` 从未发布（他人 zhu1090093659 账号，非本地能修）。

### 四、3 设计稿 + 混合状态管理
- AGENTGIT 集成草案 / JEV 替代方案 / 会话存档竞品调研 → 评估无敏感 → commit `978a911`（3 文件）。
- 混合状态（开源/开发中/收费）管理建议：敏感层分 3 类——开源公开→main；开发中→feature branch 不 merge；收费增值→private repo 或 .gitignore。

### 五、两份 DeepSeek 评价核实结论（新会话必读）
- **文件1（文档收口）**：核心主张 **大半失真**——称 INDEX 停 09-16（**实测 09-17**）、test-cases 635/169 滞后（**真值 770，非它说的 701**）、AGENTS.md 6 条铁律/R2 已修/验收有条件通过 都对。**唯一成立的实质缺口**=INDEX + test-cases **数字同步滞后**（违反 §4 铁律）+ consistency-report 待复核。→ 可执行 P1。
- **文件2（DSH 术语冲突）**：主张需核实——称被 awesome-deepseek-harness **PR #522 移除**（理由：README 把 DSH 解释为 "Declarative Skill Hub" 非 "DeepSeek Harness"，0 次提及 deepseek-harness → 巧合 match 移除）。**新会话第一步必须核实**：① 查 awesome 仓库 PR #522 真伪；② 查本项目 README/`.dsh/skills/` 里 DSH 到底指啥；③ bundle-design `dsh-plugin-model-proxy` 参照。若属实=**定位层一票否决**，需统一术语；若 PR 不实/话题本就巧合，则降级。

### 六、给新会话的判据
- git **完全同步**（67b1e06 = origin/main，工作区干净，无未 commit/push）。
- D5/D6-a/D7-pre **都已在 origin/main**，ROADMAP 里"本地未 push"标注本身过时（需订正）。
- D6-b / D4 Step3 = 人类决策卡点：D6-b（健康决定真实路由）卡 `PROXY_ROUTE_OVERRIDE=1` 默认关；D4 翻默认属 sign-off。
- 数字必须真跑（AGENTS §4）；test-cases.md 635 是滞后的，真值 770。
- DeepSeek 评价两份都要先核实再排优先级，不准照单全收。
